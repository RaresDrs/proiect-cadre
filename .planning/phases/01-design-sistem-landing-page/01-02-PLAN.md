---
phase: 01-design-sistem-landing-page
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - frontend/package.json
  - frontend/src/App.tsx
  - frontend/src/components/sections/HeroSection.tsx
  - frontend/src/components/sections/FeaturesSection.tsx
  - frontend/src/components/sections/PricingSection.tsx
  - frontend/src/components/sections/FAQSection.tsx
  - frontend/src/components/sections/CTASection.tsx
  - frontend/src/components/sections/EmailCapture.tsx
  - frontend/src/components/sections/StructuralDiagram.tsx
  - frontend/src/hooks/useInView.ts
  - frontend/src/__tests__/landing-sections.test.tsx
  - frontend/src/__tests__/EmailCapture.test.tsx
  - frontend/src/__tests__/reducedMotion.test.tsx
autonomous: true
requirements: [REQ-1a, REQ-1d, REQ-1h, REQ-1i]

must_haves:
  truths:
    - "All 5 sections render with correct id attributes: hero, features, pricing, faq, cta"
    - "Hero headline is an <h1> tag — exactly one per page"
    - "Hero section uses Framer Motion (motion package) with LazyMotion + domAnimation for lazy loading"
    - "Non-hero sections use tw-animate-css animate-fade-in-up triggered by IntersectionObserver"
    - "EmailCapture saves valid email to localStorage key 'structcalc-waitlist'"
    - "EmailCapture shows error message from i18n key 'email.error' on invalid email"
    - "prefers-reduced-motion respected: no CSS animations or motion transitions fire when media query is active"
    - "FAQ accordion uses @base-ui/react Collapsible, single-open, with aria-expanded on trigger"
    - "Pricing section shows 3 tiers: Free (visible), Pro (blurred), Enterprise (blurred) with lock icon"
  artifacts:
    - path: "frontend/src/components/sections/HeroSection.tsx"
      provides: "Hero with Framer Motion stagger entrance"
      exports: ["HeroSection"]
    - path: "frontend/src/components/sections/FeaturesSection.tsx"
      provides: "3-card features grid with scroll fade-in"
      exports: ["FeaturesSection"]
    - path: "frontend/src/components/sections/PricingSection.tsx"
      provides: "Pricing tiers + EmailCapture"
      exports: ["PricingSection"]
    - path: "frontend/src/components/sections/FAQSection.tsx"
      provides: "FAQ accordion using @base-ui/react"
      exports: ["FAQSection"]
    - path: "frontend/src/components/sections/CTASection.tsx"
      provides: "Full-width CTA band"
      exports: ["CTASection"]
    - path: "frontend/src/components/sections/EmailCapture.tsx"
      provides: "Email capture with localStorage"
      exports: ["EmailCapture"]
    - path: "frontend/src/components/sections/StructuralDiagram.tsx"
      provides: "Animated SVG beam with moment diagram"
      exports: ["StructuralDiagram"]
    - path: "frontend/src/hooks/useInView.ts"
      provides: "IntersectionObserver hook for scroll animations"
      exports: ["useInView"]
    - path: "frontend/src/App.tsx"
      provides: "Root component wiring all sections"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "all section components"
      via: "import and render in order: Navbar, HeroSection, FeaturesSection, PricingSection, FAQSection, CTASection, Footer"
      pattern: "HeroSection|FeaturesSection|PricingSection|FAQSection|CTASection"
    - from: "frontend/src/components/sections/HeroSection.tsx"
      to: "motion/react LazyMotion"
      via: "import { LazyMotion, domAnimation, m } from 'motion/react'"
      pattern: "LazyMotion.*domAnimation"
    - from: "frontend/src/components/sections/EmailCapture.tsx"
      to: "localStorage key 'structcalc-waitlist'"
      via: "localStorage.setItem('structcalc-waitlist', ...)"
      pattern: "structcalc-waitlist"
    - from: "frontend/src/components/sections/FAQSection.tsx"
      to: "@base-ui/react Collapsible"
      via: "import { Collapsible } from '@base-ui/react/collapsible'"
      pattern: "@base-ui/react"
---

<objective>
Build all 5 landing page sections (Hero, Features, Pricing, FAQ, CTA) with animations, wire them into App.tsx, and complete the test stubs from Wave 0.

Purpose: This is the visible product — after this plan, the landing page is functionally complete with all sections, animations, and interactions working.
Output: HeroSection with Framer Motion lazy-loaded animations, 3 features cards, Pricing with blur tiers, FAQ accordion, CTA, EmailCapture with localStorage, StructuralDiagram SVG, App.tsx wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-sistem-landing-page/01-CONTEXT.md
@.planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md
@.planning/phases/01-design-sistem-landing-page/01-VALIDATION.md
@.planning/phases/01-design-sistem-landing-page/01-01-SUMMARY.md

<interfaces>
<!-- From 01-01 (Wave 0 outputs) — read these before implementing -->

From frontend/src/hooks/useTheme.ts:
```typescript
export function useTheme(): { theme: 'light' | 'dark', toggleTheme: () => void }
```

From frontend/src/hooks/useLang.ts:
```typescript
export function useLang(): { lang: 'ro' | 'en', toggleLang: () => void, t: (key: string) => string }
// t('hero.headline') → 'Calculul structural, reinventat' (when lang='ro')
// t('email.error') → 'Introdu o adresă de email validă.'
// t('email.success') → 'Ești pe listă! Te anunțăm când lansăm.'
```

From frontend/src/components/layout/Navbar.tsx:
```typescript
export function Navbar(): JSX.Element
// No props — reads theme and lang from hooks internally
```

From frontend/src/components/layout/Footer.tsx:
```typescript
export function Footer(): JSX.Element
```

From frontend/src/components/ui/button.tsx:
```typescript
// Variants: default (accent fill), outline, ghost
// Sizes: sm, lg, icon
import { Button } from '@/components/ui/button'
```

From frontend/src/index.css brand tokens (CSS vars):
```
--brand-bg: #F5F5F7 (light) / #000000 (dark)
--brand-card: #FFFFFF (light) / #111111 (dark)
--brand-accent: #2563EB (light) / #3B82F6 (dark)
--brand-text: #1D1D1F (light) / #F5F5F7 (dark)
--brand-muted: #6E6E73
--brand-border: #E5E5EA (light) / #2A2A2A (dark)
--brand-destructive: #DC2626 (light) / #F87171 (dark)
--space-xs: 4px, --space-sm: 8px, --space-md: 16px, --space-lg: 24px, --space-xl: 32px, --space-2xl: 48px, --space-3xl: 64px
```

Motion package (install required — NOT framer-motion):
```typescript
// Install: npm install motion
// LazyMotion pattern for small bundle (reduces from 34KB to 4.6KB initial):
import { LazyMotion, domAnimation, m } from 'motion/react'
// Use m.div instead of motion.div inside <LazyMotion features={domAnimation}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion + create useInView hook + build HeroSection with StructuralDiagram</name>
  <read_first>
    - frontend/package.json (confirm motion not installed yet)
    - .planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md (Animation Contract section — exact durations, delays, spring values for Hero)
    - .planning/phases/01-design-sistem-landing-page/01-CONTEXT.md (D-08, D-09, D-11, D-12, D-17, D-18)
    - frontend/src/__tests__/reducedMotion.test.tsx (implement against these)
  </read_first>
  <files>
    frontend/package.json,
    frontend/src/hooks/useInView.ts,
    frontend/src/components/sections/StructuralDiagram.tsx,
    frontend/src/components/sections/HeroSection.tsx,
    frontend/src/__tests__/reducedMotion.test.tsx
  </files>
  <action>
**Step 1 — Install motion:**
```bash
cd frontend && npm install motion
```

**Step 2 — Create `frontend/src/hooks/useInView.ts`:**
```typescript
import { useEffect, useRef, useState } from 'react'

interface UseInViewOptions {
  threshold?: number
  once?: boolean
}

export function useInView({ threshold = 0.15, once = true }: UseInViewOptions = {}) {
  const ref = useRef<HTMLElement | null>(null)
  const [isInView, setIsInView] = useState(false)

  useEffect(() => {
    const element = ref.current
    if (!element) return

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsInView(true)
          if (once) observer.unobserve(element)
        }
      },
      { threshold }
    )

    observer.observe(element)
    return () => observer.disconnect()
  }, [threshold, once])

  return { ref, isInView }
}
```

**Step 3 — Create `frontend/src/components/sections/StructuralDiagram.tsx`:**
SVG of a simply-supported beam with animated moment diagram using CSS stroke-dasharray. The animation draws the moment curve from left to right when the component mounts. Respects prefers-reduced-motion by setting animation-duration to 0.001ms (handled globally in index.css).

```tsx
export function StructuralDiagram() {
  return (
    <div
      aria-hidden="true"
      role="img"
      className="w-full max-w-[480px] mx-auto"
    >
      <svg
        viewBox="0 0 480 240"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        className="w-full h-auto"
      >
        {/* Beam — horizontal line */}
        <line
          x1="60" y1="120" x2="420" y2="120"
          stroke="var(--brand-text)" strokeWidth="3" strokeLinecap="round"
        />

        {/* Left support — triangle pin */}
        <polygon
          points="60,120 45,148 75,148"
          stroke="var(--brand-text)" strokeWidth="2" fill="none"
        />
        <line x1="38" y1="152" x2="82" y2="152" stroke="var(--brand-text)" strokeWidth="2" />

        {/* Right support — roller (circle on triangle) */}
        <polygon
          points="420,120 405,148 435,148"
          stroke="var(--brand-text)" strokeWidth="2" fill="none"
        />
        <circle cx="420" cy="155" r="5" stroke="var(--brand-text)" strokeWidth="2" fill="none" />
        <line x1="398" y1="164" x2="442" y2="164" stroke="var(--brand-text)" strokeWidth="2" />

        {/* Distributed load arrows (5 arrows) */}
        {[96, 144, 192, 240, 288, 336, 384].map((x, i) => (
          <g key={i}>
            <line x1={x} y1="80" x2={x} y2="116" stroke="var(--brand-accent)" strokeWidth="1.5" />
            <polygon points={`${x},120 ${x-4},110 ${x+4},110`} fill="var(--brand-accent)" />
          </g>
        ))}
        {/* Load line across top */}
        <line x1="60" y1="80" x2="420" y2="80" stroke="var(--brand-accent)" strokeWidth="1.5" strokeDasharray="4 2" />

        {/* Moment diagram — parabolic arc (path drawn with CSS animation) */}
        {/* Parabola: M 60,120 Q 240,200 420,120 — bending below beam for positive moment */}
        <path
          d="M 60,120 Q 240,195 420,120"
          stroke="var(--brand-accent)" strokeWidth="2.5"
          fill="none" strokeLinecap="round"
          strokeDasharray="500"
          strokeDashoffset="500"
          style={{
            animation: 'drawBeamDiagram 1.2s ease-out 0.4s forwards',
          }}
        />

        {/* M(x) label */}
        <text
          x="245" y="210"
          fontSize="12" fill="var(--brand-muted)"
          textAnchor="middle" fontFamily="var(--font-sans)"
          opacity="0"
          style={{ animation: 'fadeInLabel 0.4s ease-out 1.4s forwards' }}
        >
          M(x)
        </text>
      </svg>

      <style>{`
        @keyframes drawBeamDiagram {
          to { stroke-dashoffset: 0; }
        }
        @keyframes fadeInLabel {
          to { opacity: 1; }
        }
      `}</style>
    </div>
  )
}
```

**Step 4 — Create `frontend/src/components/sections/HeroSection.tsx`:**
Uses LazyMotion + domAnimation from 'motion/react' per D-08 (lazy loaded, not in main bundle). Spring physics per D-09. Animation values from UI-SPEC Animation Contract:
- Headline: fade-in + translateY(20px → 0), 500ms, delay 0ms, spring stiffness 100 damping 20
- Sub-headline: same, delay 150ms
- CTA buttons: fade-in + translateY(16px → 0), 400ms, delay 300ms, spring stiffness 120 damping 22
- Structural diagram: fade-in + scale(0.95 → 1.0), 600ms, delay 200ms, spring stiffness 80 damping 18

```tsx
import React, { Suspense } from 'react'
import { Button } from '@/components/ui/button'
import { useLang } from '@/hooks/useLang'
import { StructuralDiagram } from './StructuralDiagram'

// Lazy-load Framer Motion (motion/react) — keeps initial bundle small (D-08)
const MotionHeroContent = React.lazy(() =>
  import('motion/react').then(({ LazyMotion, domAnimation, m }) => ({
    default: function MotionHeroContent({ t }: { t: (k: string) => string }) {
      const scrollTo = (id: string) =>
        document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })
      return (
        <LazyMotion features={domAnimation}>
          <m.h1
            className="text-[clamp(36px,5vw,56px)] font-bold leading-[1.1]
                       text-[var(--brand-text)] max-w-[640px]"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ type: 'spring', stiffness: 100, damping: 20, delay: 0 }}
          >
            {t('hero.headline')}
          </m.h1>
          <m.p
            className="mt-4 text-[clamp(18px,2vw,20px)] font-normal leading-[1.5]
                       text-[var(--brand-muted)] max-w-[560px]"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ type: 'spring', stiffness: 100, damping: 20, delay: 0.15 }}
          >
            {t('hero.subheadline')}
          </m.p>
          <m.div
            className="mt-8 flex flex-wrap gap-4"
            initial={{ opacity: 0, y: 16 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ type: 'spring', stiffness: 120, damping: 22, delay: 0.3 }}
          >
            <Button
              size="lg"
              onClick={() => scrollTo('cta')}
              className="bg-[var(--brand-accent)] text-white hover:bg-[var(--brand-accent)]/90
                         transition-colors duration-150 min-h-[44px] px-6"
            >
              {t('hero.cta.primary')}
            </Button>
            <Button
              variant="outline"
              size="lg"
              onClick={() => scrollTo('features')}
              className="min-h-[44px] px-6 transition-colors duration-150"
            >
              {t('hero.cta.secondary')}
            </Button>
          </m.div>
        </LazyMotion>
      )
    },
  }))
)

// Static fallback for when motion is loading or prefers-reduced-motion
function StaticHeroContent({ t }: { t: (k: string) => string }) {
  const scrollTo = (id: string) =>
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })
  return (
    <>
      <h1
        className="text-[clamp(36px,5vw,56px)] font-bold leading-[1.1]
                   text-[var(--brand-text)] max-w-[640px]"
      >
        {t('hero.headline')}
      </h1>
      <p
        className="mt-4 text-[clamp(18px,2vw,20px)] font-normal leading-[1.5]
                   text-[var(--brand-muted)] max-w-[560px]"
      >
        {t('hero.subheadline')}
      </p>
      <div className="mt-8 flex flex-wrap gap-4">
        <Button
          size="lg"
          onClick={() => scrollTo('cta')}
          className="bg-[var(--brand-accent)] text-white hover:bg-[var(--brand-accent)]/90
                     transition-colors duration-150 min-h-[44px] px-6"
        >
          {t('hero.cta.primary')}
        </Button>
        <Button
          variant="outline"
          size="lg"
          onClick={() => scrollTo('features')}
          className="min-h-[44px] px-6 transition-colors duration-150"
        >
          {t('hero.cta.secondary')}
        </Button>
      </div>
    </>
  )
}

export function HeroSection() {
  const { t } = useLang()
  const prefersReducedMotion =
    typeof window !== 'undefined' &&
    window.matchMedia('(prefers-reduced-motion: reduce)').matches

  return (
    <section
      id="hero"
      className="min-h-[100svh] lg:min-h-0 lg:py-[64px]
                 bg-[var(--brand-bg)]
                 flex items-center
                 pt-16" /* pt-16 = nav height offset */
    >
      <div
        className="max-w-[1200px] mx-auto px-6 w-full
                   grid grid-cols-1 lg:grid-cols-2 gap-12 items-center"
      >
        {/* Text content */}
        <div>
          {prefersReducedMotion ? (
            <StaticHeroContent t={t} />
          ) : (
            <Suspense fallback={<StaticHeroContent t={t} />}>
              <MotionHeroContent t={t} />
            </Suspense>
          )}
        </div>

        {/* Structural diagram */}
        <div className="hidden lg:flex justify-center">
          <StructuralDiagram />
        </div>
      </div>
    </section>
  )
}
```

**Step 5 — Fill in `frontend/src/__tests__/reducedMotion.test.tsx` with real tests:**
```tsx
import { render } from '@testing-library/react'
import { describe, it, expect, beforeEach, vi } from 'vitest'

// Mock window.matchMedia to simulate prefers-reduced-motion: reduce
function mockReducedMotion(value: boolean) {
  Object.defineProperty(window, 'matchMedia', {
    writable: true,
    value: (query: string) => ({
      matches: query === '(prefers-reduced-motion: reduce)' ? value : false,
      media: query,
      onchange: null,
      addListener: vi.fn(),
      removeListener: vi.fn(),
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      dispatchEvent: vi.fn(),
    }),
  })
}

describe('prefers-reduced-motion — REQ-1h', () => {
  it('HeroSection renders static content (no Suspense lazy motion) when prefers-reduced-motion is reduce', async () => {
    mockReducedMotion(true)
    const { HeroSection } = await import('@/components/sections/HeroSection')
    const { container } = render(<HeroSection />)
    // When reduced motion, StaticHeroContent is rendered — h1 is immediately in DOM
    expect(container.querySelector('h1')).not.toBeNull()
  })

  it('HeroSection renders animated content (Suspense wrapper) when motion is allowed', async () => {
    mockReducedMotion(false)
    const { HeroSection } = await import('@/components/sections/HeroSection')
    const { container } = render(<HeroSection />)
    // Suspense fallback renders StaticHeroContent initially — h1 is present
    expect(container.querySelector('h1')).not.toBeNull()
  })
})
```
  </action>
  <verify>
    <automated>cd /c/Users/Rares/proiect-cadre/frontend && npx vitest run src/__tests__/reducedMotion.test.tsx --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` dependencies contains `"motion"`
    - `frontend/src/hooks/useInView.ts` exists and exports `useInView`
    - `frontend/src/components/sections/HeroSection.tsx` contains `LazyMotion` and `domAnimation` from `'motion/react'`
    - `frontend/src/components/sections/HeroSection.tsx` contains `React.lazy(` (lazy loading of motion)
    - `frontend/src/components/sections/HeroSection.tsx` contains `prefers-reduced-motion`
    - `frontend/src/components/sections/HeroSection.tsx` contains `id="hero"`
    - `frontend/src/components/sections/StructuralDiagram.tsx` contains `drawBeamDiagram` (CSS keyframe animation)
    - `frontend/src/components/sections/StructuralDiagram.tsx` contains `stroke-dasharray`
    - `cd frontend && npx vitest run src/__tests__/reducedMotion.test.tsx` exits 0
  </acceptance_criteria>
  <done>motion installed, useInView hook created, HeroSection renders with lazy-loaded Framer Motion + static fallback for prefers-reduced-motion, StructuralDiagram is an animated SVG beam, reducedMotion tests pass.</done>
</task>

<task type="tdd">
  <name>Task 2: Build FeaturesSection, FAQSection, CTASection + landing-sections test stubs</name>
  <read_first>
    - .planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md (Component Inventory, Copywriting Contract, Animation Contract for non-Hero)
    - .planning/phases/01-design-sistem-landing-page/01-CONTEXT.md (D-14, D-19, D-20, D-21, D-22)
    - frontend/src/hooks/useInView.ts (use for scroll animations)
    - frontend/src/__tests__/landing-sections.test.tsx (implement against these)
  </read_first>
  <files>
    frontend/src/components/sections/FeaturesSection.tsx,
    frontend/src/components/sections/FAQSection.tsx,
    frontend/src/components/sections/CTASection.tsx,
    frontend/src/__tests__/landing-sections.test.tsx
  </files>
  <behavior>
    - All sections render with correct id attributes (features, faq, cta)
    - FeaturesSection has heading via t('features.heading') and 3 FeatureCard children
    - FAQSection uses @base-ui/react Collapsible with aria-expanded on trigger, single-open behavior
    - CTASection has id="cta" and a button that scrolls to top
  </behavior>
  <action>
**Step 1 — Create `frontend/src/components/sections/FeaturesSection.tsx`:**
3 feature cards, scroll-triggered fade-in via tw-animate-css + useInView. Icons from lucide-react (Ruler, FileText, Smartphone). Use animate-fade-in-up with delay stagger (0ms, 100ms, 200ms). Each card wrapped in FeatureCard sub-component with its own useInView ref.

**Step 2 — Create `frontend/src/components/sections/FAQSection.tsx`:**
Uses @base-ui/react Collapsible for accordion. Single-open behavior via openItem state. aria-expanded on Collapsible.Trigger. 4 FAQ items from i18n keys faq.q1-q4, faq.a1-a4. ChevronDown icon rotates 180deg when open.

**Step 3 — Create `frontend/src/components/sections/CTASection.tsx`:**
Full-width blue accent band (bg-[var(--brand-accent)]). White heading + body text. Button that calls window.scrollTo({ top: 0, behavior: 'smooth' }).

**Step 4 — Create temporary PricingSection stub so App and tests compile:**
Create `frontend/src/components/sections/PricingSection.tsx` with stub content:
```tsx
export function PricingSection() { return <section id="pricing" /> }
```
Task 3 will replace this stub with the full implementation.

**Step 5 — Fill test stubs in `landing-sections.test.tsx`:**
```tsx
import { render } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import App from '@/App'

describe('Landing sections — REQ-1a', () => {
  it('renders section#hero', () => {
    const { container } = render(<App />)
    expect(container.querySelector('#hero')).not.toBeNull()
  })
  it('renders section#features', () => {
    const { container } = render(<App />)
    expect(container.querySelector('#features')).not.toBeNull()
  })
  it('renders section#pricing', () => {
    const { container } = render(<App />)
    expect(container.querySelector('#pricing')).not.toBeNull()
  })
  it('renders section#faq', () => {
    const { container } = render(<App />)
    expect(container.querySelector('#faq')).not.toBeNull()
  })
  it('renders section#cta', () => {
    const { container } = render(<App />)
    expect(container.querySelector('#cta')).not.toBeNull()
  })
  it('renders footer', () => {
    const { container } = render(<App />)
    expect(container.querySelector('footer')).not.toBeNull()
  })
  it('has exactly one h1', () => {
    const { container } = render(<App />)
    expect(container.querySelectorAll('h1')).toHaveLength(1)
  })
})
```
  </action>
  <verify>
    <automated>cd /c/Users/Rares/proiect-cadre/frontend && npx vitest run src/__tests__/landing-sections.test.tsx --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/sections/FeaturesSection.tsx` exports FeaturesSection with id="features"
    - `frontend/src/components/sections/FAQSection.tsx` contains `@base-ui/react/collapsible` and `aria-expanded={isOpen}`
    - `frontend/src/components/sections/CTASection.tsx` exports CTASection with id="cta"
    - `cd frontend && npx vitest run src/__tests__/landing-sections.test.tsx` exits 0
  </acceptance_criteria>
  <done>FeaturesSection, FAQSection, CTASection created; landing-sections test stubs filled and passing.</done>
</task>

<task type="tdd">
  <name>Task 3: Build PricingSection + EmailCapture + wire App.tsx + complete all tests</name>
  <read_first>
    - .planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md (Copywriting Contract for email strings, Pricing tier spec)
    - .planning/phases/01-design-sistem-landing-page/01-CONTEXT.md (D-19, D-20, D-21, D-22)
    - frontend/src/__tests__/EmailCapture.test.tsx (implement against these)
    - frontend/src/App.tsx (current state — will be replaced)
    - frontend/src/components/layout/Navbar.tsx (import for App.tsx)
    - frontend/src/components/layout/Footer.tsx (import for App.tsx)
    - frontend/src/components/sections/FeaturesSection.tsx (confirm exists from Task 2)
    - frontend/src/components/sections/FAQSection.tsx (confirm exists from Task 2)
    - frontend/src/components/sections/CTASection.tsx (confirm exists from Task 2)
  </read_first>
  <files>
    frontend/src/components/sections/EmailCapture.tsx,
    frontend/src/components/sections/PricingSection.tsx,
    frontend/src/App.tsx,
    frontend/src/__tests__/EmailCapture.test.tsx
  </files>
  <behavior>
    - EmailCapture: submitting valid email (contains '@') saves to localStorage.setItem('structcalc-waitlist', email) and shows success state
    - EmailCapture: submitting invalid email (no '@') shows error: t('email.error') = 'Introdu o adresă de email validă.'
    - EmailCapture: empty submit does NOT call localStorage.setItem
    - PricingSection imports EmailCapture and renders it below the 3 tiers
    - App.tsx imports all 5 sections (Hero, Features, Pricing, FAQ, CTA) and renders them in order
  </behavior>
  <action>
**Step 1 — Create `frontend/src/components/sections/EmailCapture.tsx`:**
Form with email validation (/^[^\s@]+@[^\s@]+\.[^\s@]+$/). On valid submit: localStorage.setItem('structcalc-waitlist', email), show success state (role="status"). On invalid: show error (role="alert", id="email-error"), set aria-invalid="true" on input. Empty submit counts as invalid. Use useLang for i18n strings: email.placeholder, email.label, email.cta, email.success, email.error.

**Step 2 — Create full `frontend/src/components/sections/PricingSection.tsx`:**
Replaces the stub from Task 2. 3 tiers: Free (fully visible, border-accent), Pro (blur-sm + Lock icon + badge), Enterprise (blur-sm + Lock icon + badge). Badge text from t('pricing.badge') = "Disponibil Q3 2026" per D-20. EmailCapture rendered below tiers per D-21. PricingSection imports EmailCapture directly — must be in same task since PricingSection depends on EmailCapture.

**Step 3 — Rewrite `frontend/src/App.tsx`:**
Wire all sections in order per D-14: Navbar, HeroSection, FeaturesSection, PricingSection, FAQSection, CTASection, Footer.
```tsx
import { Navbar } from '@/components/layout/Navbar'
import { Footer } from '@/components/layout/Footer'
import { HeroSection } from '@/components/sections/HeroSection'
import { FeaturesSection } from '@/components/sections/FeaturesSection'
import { PricingSection } from '@/components/sections/PricingSection'
import { FAQSection } from '@/components/sections/FAQSection'
import { CTASection } from '@/components/sections/CTASection'

function App() {
  return (
    <div className="bg-[var(--brand-bg)] text-[var(--brand-text)] min-h-screen">
      <Navbar />
      <main>
        <HeroSection />
        <FeaturesSection />
        <PricingSection />
        <FAQSection />
        <CTASection />
      </main>
      <Footer />
    </div>
  )
}

export default App
```

**Step 4 — Fill in `EmailCapture.test.tsx`:**
```tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, beforeEach } from 'vitest'
import { EmailCapture } from '@/components/sections/EmailCapture'

describe('EmailCapture — REQ-1i', () => {
  beforeEach(() => {
    localStorage.clear()
  })

  it('submitting valid email saves to localStorage key "structcalc-waitlist"', () => {
    render(<EmailCapture />)
    const input = screen.getByRole('textbox')
    fireEvent.change(input, { target: { value: 'test@example.com' } })
    fireEvent.submit(input.closest('form')!)
    expect(localStorage.getItem('structcalc-waitlist')).toBe('test@example.com')
  })

  it('submitting invalid email shows error message', () => {
    render(<EmailCapture />)
    const input = screen.getByRole('textbox')
    fireEvent.change(input, { target: { value: 'notanemail' } })
    fireEvent.submit(input.closest('form')!)
    expect(screen.getByRole('alert').textContent).toContain('Introdu o adresă de email validă')
    expect(localStorage.getItem('structcalc-waitlist')).toBeNull()
  })

  it('after successful submit shows success message', () => {
    render(<EmailCapture />)
    const input = screen.getByRole('textbox')
    fireEvent.change(input, { target: { value: 'valid@test.com' } })
    fireEvent.submit(input.closest('form')!)
    expect(screen.getByRole('status').textContent).toContain('Ești pe listă')
  })

  it('does not save to localStorage on empty submit', () => {
    render(<EmailCapture />)
    const form = document.querySelector('form')!
    fireEvent.submit(form)
    expect(localStorage.getItem('structcalc-waitlist')).toBeNull()
  })
})
```

**Step 5 — Run full Vitest suite and fix any TypeScript errors:**
```bash
cd frontend && npx vitest run --reporter=dot && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /c/Users/Rares/proiect-cadre/frontend && npx vitest run --reporter=dot 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/sections/EmailCapture.tsx` contains `localStorage.setItem('structcalc-waitlist'`
    - `frontend/src/components/sections/PricingSection.tsx` contains `id="pricing"` and `blur-sm` (locked tiers)
    - `frontend/src/App.tsx` contains HeroSection, FeaturesSection, PricingSection, FAQSection, CTASection
    - `cd frontend && npx vitest run src/__tests__/EmailCapture.test.tsx` exits 0
    - `cd frontend && npx vitest run --reporter=dot` exits 0 (full suite green)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 5 landing sections render, FAQAccordion uses @base-ui/react, EmailCapture saves to localStorage, App.tsx wires all sections, full Vitest suite green.</done>
</task>


</tasks>

<verification>
After all tasks complete:

1. `cd frontend && npx vitest run` exits 0 — all tests pass
2. `cd frontend && npx tsc --noEmit` exits 0 — no TypeScript errors
3. `grep "id=\"hero\"" frontend/src/components/sections/HeroSection.tsx` — found
4. `grep "id=\"features\"" frontend/src/components/sections/FeaturesSection.tsx` — found
5. `grep "id=\"pricing\"" frontend/src/components/sections/PricingSection.tsx` — found
6. `grep "id=\"faq\"" frontend/src/components/sections/FAQSection.tsx` — found
7. `grep "id=\"cta\"" frontend/src/components/sections/CTASection.tsx` — found
8. `grep "LazyMotion" frontend/src/components/sections/HeroSection.tsx` — found
9. `grep "structcalc-waitlist" frontend/src/components/sections/EmailCapture.tsx` — found
10. `cd frontend && npm run dev` — dev server starts without errors
</verification>

<success_criteria>
- motion package installed and HeroSection uses LazyMotion + domAnimation lazy-loaded
- StructuralDiagram SVG has animated stroke-dasharray beam + moment diagram
- All 5 sections have correct id attributes: hero, features, pricing, faq, cta
- prefers-reduced-motion respected in HeroSection (StaticHeroContent fallback)
- EmailCapture stores to localStorage key 'structcalc-waitlist', shows RO error on invalid
- FAQ uses @base-ui/react Collapsible with aria-expanded
- Pricing shows Free visible + Pro/Enterprise blurred with Lock icon
- Full Vitest suite green (6 test files, all passing)
- TypeScript 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-sistem-landing-page/01-02-SUMMARY.md` with:
- All files created
- Key implementation decisions (motion lazy loading pattern, FAQ accordion choice, email validation approach)
- Interfaces exported (component signatures for each section)
- Test results (which tests pass)
</output>
