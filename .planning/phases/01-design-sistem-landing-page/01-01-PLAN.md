---
phase: 01-design-sistem-landing-page
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/__tests__/useTheme.test.ts
  - frontend/src/__tests__/useLang.test.ts
  - frontend/src/__tests__/landing-sections.test.tsx
  - frontend/src/__tests__/EmailCapture.test.tsx
  - frontend/src/__tests__/reducedMotion.test.tsx
  - frontend/src/__tests__/responsive.test.tsx
  - frontend/src/index.css
  - frontend/index.html
  - frontend/src/lib/i18n.ts
  - frontend/src/hooks/useTheme.ts
  - frontend/src/hooks/useLang.ts
  - frontend/src/components/layout/Navbar.tsx
  - frontend/src/components/layout/Footer.tsx
autonomous: true
scope_exception: "15 files listed but 8 are trivial it.todo test stubs (~15 lines each). Substantive files: vitest.config.ts, index.css, index.html, i18n.ts, useTheme.ts, useLang.ts, Navbar.tsx, Footer.tsx. Total token context is well within 50% budget."
requirements: [REQ-1b, REQ-1j, REQ-1l]

must_haves:
  truths:
    - "Vitest runs and finds test files — `cd frontend && npx vitest run` exits 0 (tests pass or are pending stubs)"
    - "Dark mode CSS tokens exist in index.css with exact hex values from design contract"
    - "useTheme hook reads/writes to localStorage key `structcalc-theme` and toggles `.dark` on `<html>`"
    - "useLang hook reads/writes to localStorage key `structcalc-lang` and updates `<html lang>` attribute"
    - "Translation object in i18n.ts exports all 30+ string keys for both 'ro' and 'en'"
    - "Navbar component renders sticky nav with logo, nav links, DarkModeToggle, and LanguageToggle"
    - "Footer component renders copyright line and nav links"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest config with jsdom environment"
      contains: "environment: 'jsdom'"
    - path: "frontend/src/__tests__/useTheme.test.ts"
      provides: "Test stubs for REQ-1b dark mode toggle"
    - path: "frontend/src/__tests__/useLang.test.ts"
      provides: "Test stubs for REQ-1j i18n and REQ-1l html lang"
    - path: "frontend/src/__tests__/landing-sections.test.tsx"
      provides: "Test stubs for REQ-1a landing sections render"
    - path: "frontend/src/__tests__/EmailCapture.test.tsx"
      provides: "Test stubs for REQ-1i email localStorage"
    - path: "frontend/src/__tests__/reducedMotion.test.tsx"
      provides: "Test stubs for REQ-1h reduced motion"
    - path: "frontend/src/__tests__/responsive.test.tsx"
      provides: "Test stubs for REQ-1g 320px viewport"
    - path: "frontend/src/lib/i18n.ts"
      provides: "Translation object RO/EN"
      exports: ["translations", "Lang"]
    - path: "frontend/src/hooks/useTheme.ts"
      provides: "Dark mode hook"
      exports: ["useTheme"]
    - path: "frontend/src/hooks/useLang.ts"
      provides: "i18n hook"
      exports: ["useLang"]
    - path: "frontend/src/components/layout/Navbar.tsx"
      provides: "Sticky navigation bar"
      exports: ["Navbar"]
    - path: "frontend/src/components/layout/Footer.tsx"
      provides: "Site footer"
      exports: ["Footer"]
  key_links:
    - from: "frontend/src/hooks/useTheme.ts"
      to: "localStorage key 'structcalc-theme'"
      via: "localStorage.setItem / getItem"
      pattern: "structcalc-theme"
    - from: "frontend/src/hooks/useLang.ts"
      to: "document.documentElement.lang"
      via: "document.documentElement.setAttribute('lang', ...)"
      pattern: "setAttribute.*lang"
    - from: "frontend/src/components/layout/Navbar.tsx"
      to: "frontend/src/hooks/useTheme.ts"
      via: "import useTheme"
      pattern: "useTheme"
    - from: "frontend/src/components/layout/Navbar.tsx"
      to: "frontend/src/hooks/useLang.ts"
      via: "import useLang"
      pattern: "useLang"
---

<objective>
Install test infrastructure, extend design tokens, and build shared hooks + layout components that all subsequent plans depend on.

Purpose: Wave 0 creates the foundation — test stubs must exist so Wave 1 tasks have automated verification targets. Design tokens and shared hooks prevent Wave 1 plans from making incompatible choices.
Output: vitest.config.ts + 6 test stub files, extended index.css with brand OKLCH tokens, useTheme hook, useLang hook, i18n.ts, Navbar and Footer components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-design-sistem-landing-page/01-CONTEXT.md
@.planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md
@.planning/phases/01-design-sistem-landing-page/01-VALIDATION.md

<interfaces>
<!-- Existing code the executor needs. Read-only — do not modify these contracts. -->

From frontend/src/components/ui/button.tsx:
```typescript
// Uses @base-ui/react/button primitive
// Variants: default (accent fill), outline, secondary, ghost, destructive, link
// Sizes: default, xs, sm, lg, icon, icon-xs, icon-sm, icon-lg
import { Button } from "@/components/ui/button"
// Usage: <Button variant="outline" size="lg">Text</Button>
```

From frontend/src/index.css (current structure — EXTEND, do NOT rewrite):
```css
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";
@import "@fontsource-variable/geist";

@custom-variant dark (&:is(.dark *));

@layer base {
  :root {
    /* existing OKLCH tokens — do not remove */
    --background: oklch(...);
    /* ... */
  }
  .dark {
    /* existing dark tokens — do not remove */
  }
}

@theme inline {
  --font-sans: 'Geist Variable', sans-serif;
  /* ... */
}
```

From frontend/vite.config.ts:
```typescript
// alias "@" → "./src"
// plugins: [react(), tailwindcss()]
// No test config here — vitest.config.ts is separate
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + create vitest.config.ts + write all 6 test stub files</name>
  <read_first>
    - frontend/package.json (current deps — confirm vitest absent before install)
    - frontend/vite.config.ts (alias "@" → "./src" must be replicated in vitest config)
    - .planning/phases/01-design-sistem-landing-page/01-VALIDATION.md (exact test file paths and what each covers)
  </read_first>
  <files>
    frontend/package.json,
    frontend/vitest.config.ts,
    frontend/src/__tests__/useTheme.test.ts,
    frontend/src/__tests__/useLang.test.ts,
    frontend/src/__tests__/landing-sections.test.tsx,
    frontend/src/__tests__/EmailCapture.test.tsx,
    frontend/src/__tests__/reducedMotion.test.tsx,
    frontend/src/__tests__/responsive.test.tsx
  </files>
  <action>
**Step 1 — Install test dependencies:**
```bash
cd frontend && npm install --save-dev vitest @testing-library/react @testing-library/user-event jsdom @vitest/coverage-v8
```

**Step 2 — Create `frontend/vitest.config.ts`:**
```typescript
import path from 'path'
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

**Step 3 — Create test stub files.** Each file must be a FAILING stub (describe block exists, tests are `todo` or `expect.soft` placeholders) so the suite runs without crashing. The actual implementations come in Tasks 2 and 3 of this plan and Wave 1 plans.

`frontend/src/__tests__/useTheme.test.ts` (covers REQ-1b):
```typescript
import { describe, it, expect, beforeEach } from 'vitest'

describe('useTheme — REQ-1b', () => {
  beforeEach(() => {
    localStorage.clear()
    document.documentElement.classList.remove('dark')
  })

  it.todo('toggleTheme adds .dark class to <html> when switching to dark mode')
  it.todo('toggleTheme removes .dark class from <html> when switching to light mode')
  it.todo('persists theme to localStorage key "structcalc-theme"')
  it.todo('reads "structcalc-theme" from localStorage on init')
  it.todo('defaults to system prefers-color-scheme when localStorage is empty')
})
```

`frontend/src/__tests__/useLang.test.ts` (covers REQ-1j, REQ-1l):
```typescript
import { describe, it, beforeEach } from 'vitest'

describe('useLang — REQ-1j, REQ-1l', () => {
  beforeEach(() => {
    localStorage.clear()
    document.documentElement.setAttribute('lang', 'ro')
  })

  it.todo('defaults to "ro" language')
  it.todo('toggleLang switches from ro to en')
  it.todo('toggleLang switches from en to ro')
  it.todo('persists lang to localStorage key "structcalc-lang"')
  it.todo('updates document.documentElement.lang attribute on toggle')
})
```

`frontend/src/__tests__/landing-sections.test.tsx` (covers REQ-1a):
```typescript
import { describe, it } from 'vitest'

describe('Landing sections — REQ-1a', () => {
  it.todo('renders <section id="hero">')
  it.todo('renders <section id="features">')
  it.todo('renders <section id="pricing">')
  it.todo('renders <section id="faq">')
  it.todo('renders <section id="cta">')
  it.todo('renders <footer>')
  it.todo('exactly one <h1> exists on the page')
})
```

`frontend/src/__tests__/EmailCapture.test.tsx` (covers REQ-1i):
```typescript
import { describe, it, beforeEach } from 'vitest'

describe('EmailCapture — REQ-1i', () => {
  beforeEach(() => {
    localStorage.clear()
  })

  it.todo('submitting valid email saves to localStorage key "structcalc-waitlist"')
  it.todo('submitting invalid email shows error message matching "Introdu o adresă de email validă."')
  it.todo('after successful submit shows success message matching "Ești pe listă!"')
  it.todo('does not submit or save to localStorage when email is empty')
})
```

`frontend/src/__tests__/reducedMotion.test.tsx` (covers REQ-1h):
```typescript
import { describe, it } from 'vitest'

describe('prefers-reduced-motion — REQ-1h', () => {
  it.todo('HeroSection renders without motion elements when prefers-reduced-motion is reduce')
  it.todo('scroll-animated sections render at final visible state when prefers-reduced-motion is reduce')
})
```

`frontend/src/__tests__/responsive.test.tsx` (covers REQ-1g):
```typescript
import { describe, it } from 'vitest'

describe('Responsive layout — REQ-1g', () => {
  it.todo('landing page container does not overflow at 320px viewport width')
  it.todo('hero headline font-size is at least 36px at 320px viewport')
})
```
  </action>
  <verify>
    <automated>cd /c/Users/Rares/proiect-cadre/frontend && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/vitest.config.ts` exists and contains `environment: 'jsdom'`
    - `frontend/src/__tests__/` directory contains exactly 6 files: useTheme.test.ts, useLang.test.ts, landing-sections.test.tsx, EmailCapture.test.tsx, reducedMotion.test.tsx, responsive.test.tsx
    - `cd frontend && npx vitest run` exits 0 (all tests are `todo` — not failing, not erroring)
    - `frontend/package.json` devDependencies contains `"vitest"`, `"@testing-library/react"`, `"jsdom"`
  </acceptance_criteria>
  <done>Vitest runs green (all todo stubs pass), 6 test files exist, vitest.config.ts has jsdom environment and "@" alias.</done>
</task>

<task type="tdd">
  <name>Task 2: Extend index.css design tokens + fix index.html + create i18n.ts + useTheme hook + useLang hook</name>
  <read_first>
    - frontend/src/index.css (FULL FILE — must see existing OKLCH structure before extending)
    - frontend/index.html (fix lang="en" → lang="ro", add dark mode blocking script)
    - .planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md (Color section: exact hex values for all brand tokens)
    - .planning/phases/01-design-sistem-landing-page/01-CONTEXT.md (D-29 through D-32 for theme; D-23 through D-25 for i18n)
    - frontend/src/__tests__/useTheme.test.ts (implement against these test expectations)
    - frontend/src/__tests__/useLang.test.ts (implement against these test expectations)
  </read_first>
  <files>
    frontend/src/index.css,
    frontend/index.html,
    frontend/src/lib/i18n.ts,
    frontend/src/hooks/useTheme.ts,
    frontend/src/hooks/useLang.ts
  </files>
  <behavior>
    - useTheme: `toggleTheme()` flips `.dark` class on `document.documentElement`
    - useTheme: persists to `localStorage.setItem('structcalc-theme', 'dark'|'light')`
    - useTheme: on init reads localStorage; if empty uses `window.matchMedia('(prefers-color-scheme: dark)').matches`
    - useLang: `toggleLang()` alternates state between 'ro' and 'en'
    - useLang: persists to `localStorage.setItem('structcalc-lang', 'ro'|'en')`
    - useLang: on toggle calls `document.documentElement.setAttribute('lang', lang)`
    - useLang: returns `{ lang, toggleLang, t }` where `t(key)` returns `translations[lang][key]`
  </behavior>
  <action>
**Step 1 — Extend `frontend/src/index.css`.**
APPEND the following inside the existing `@layer base` block, after the existing `.dark` closing brace. Do NOT remove or alter existing OKLCH tokens. Add brand token overrides in a new `@layer base` block appended at the end of the file:

```css
/* === StructCalc Brand Tokens (Phase 1) ===
   Appended after existing shadcn base tokens — do not reorder existing tokens above.
   These override the shadcn neutral palette with the Apple-style brand palette. */

@layer base {
  :root {
    /* Brand backgrounds */
    --brand-bg: #F5F5F7;          /* D-01: dominant 60% — page background */
    --brand-card: #FFFFFF;         /* secondary 30% — cards, nav */
    --brand-accent: #2563EB;       /* D-03: accent 10% — see reserved list in UI-SPEC */
    --brand-text: #1D1D1F;         /* D-02: primary text */
    --brand-muted: #6E6E73;        /* D-04: secondary/muted text */
    --brand-border: #E5E5EA;       /* card borders, dividers */
    --brand-destructive: #DC2626;  /* email validation error — light mode */

    /* Spacing scale (CSS vars for consistency) */
    --space-xs: 4px;
    --space-sm: 8px;
    --space-md: 16px;
    --space-lg: 24px;
    --space-xl: 32px;
    --space-2xl: 48px;
    --space-3xl: 64px;

    /* Typography */
    --text-body: 16px;
    --text-label: 14px;
    --text-heading-desktop: 28px;
    --text-heading-mobile: 22px;
    --text-display-desktop: 56px;
    --text-display-mobile: 36px;
  }

  .dark {
    /* Brand backgrounds — dark mode (D-07) */
    --brand-bg: #000000;           /* OLED-friendly dominant */
    --brand-card: #111111;         /* cards, nav background */
    --brand-accent: #3B82F6;       /* D-07: lightened 1 step for AAA contrast on black */
    --brand-text: #F5F5F7;         /* primary text dark */
    --brand-muted: #6E6E73;        /* same semantic role */
    --brand-border: #2A2A2A;       /* card borders dark */
    --brand-destructive: #F87171;  /* lightened for dark bg contrast */
  }

  /* Global accessibility: focus rings */
  *:focus-visible {
    outline: 2px solid var(--brand-accent);
    outline-offset: 2px;
  }

  /* Cursor pointer on all interactive elements */
  button, [role="button"], a {
    cursor: pointer;
  }
}

/* Animation: prefers-reduced-motion — disable all transitions/animations globally */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.001ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.001ms !important;
  }
}
```

**Step 2 — Fix `frontend/index.html`:**
Change `<html lang="en">` to `<html lang="ro">`. Change `<title>frontend</title>` to `<title>StructCalc — Calcul structural 2D</title>`. Add the dark mode blocking script and manifest link inside `<head>`, BEFORE the favicon link:

```html
<!doctype html>
<html lang="ro">
  <head>
    <meta charset="UTF-8" />
    <!-- Dark mode FOUC prevention — blocking script (D-31) -->
    <!-- Must be first in <head> before any CSS loads -->
    <script>
      (function() {
        var stored = localStorage.getItem('structcalc-theme');
        var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (stored === 'dark' || (!stored && prefersDark)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="manifest" href="/manifest.json" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#2563EB" />
    <title>StructCalc — Calcul structural 2D</title>
  </head>
  <body>
    <a href="#features" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-[9999] focus:px-4 focus:py-2 focus:bg-white focus:text-black focus:rounded">
      Sari la conținut
    </a>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 3 — Create `frontend/src/lib/i18n.ts`:**
```typescript
export type Lang = 'ro' | 'en'

export const translations: Record<Lang, Record<string, string>> = {
  ro: {
    // Nav
    'nav.logo': 'StructCalc',
    'nav.features': 'Funcții',
    'nav.pricing': 'Prețuri',
    'nav.faq': 'FAQ',
    'nav.cta': 'Începe gratuit',
    // Hero
    'hero.headline': 'Calculul structural, reinventat',
    'hero.subheadline': 'Software profesional pentru analiza structurilor 2D — gratuit pentru studenți și ingineri.',
    'hero.cta.primary': 'Încearcă gratuit',
    'hero.cta.secondary': 'Vezi cum funcționează',
    // Features
    'features.heading': 'De ce StructCalc?',
    'features.1.title': 'Calcul precis',
    'features.1.body': 'Algoritmi verificați pentru bare, grinzi și cadre 2D.',
    'features.2.title': 'Export PDF',
    'features.2.body': 'Rapoarte profesionale gata de prezentat.',
    'features.3.title': 'Dark mode & PWA',
    'features.3.body': 'Funcționează offline, pe orice dispozitiv.',
    // Pricing
    'pricing.heading': 'Prețuri',
    'pricing.badge': 'Disponibil Q3 2026',
    'pricing.locked': 'Deblocat în curând',
    'pricing.free.name': 'Gratuit',
    'pricing.free.tagline': 'Pentru studenți și utilizatori solo',
    // Email capture
    'email.label': 'Fii primul care află',
    'email.placeholder': 'adresa@email.com',
    'email.cta': 'Notifică-mă',
    'email.success': 'Ești pe listă! Te anunțăm când lansăm.',
    'email.error': 'Introdu o adresă de email validă.',
    // FAQ
    'faq.heading': 'Întrebări frecvente',
    'faq.q1': 'Ce tipuri de structuri pot analiza?',
    'faq.a1': 'StructCalc suportă bare, grinzi simplu rezemate, cadre 2D și structuri cu noduri rigide.',
    'faq.q2': 'Este cu adevărat gratuit?',
    'faq.a2': 'Da. Tier-ul Free nu expiră și nu necesită card.',
    'faq.q3': 'Funcționează pe mobil?',
    'faq.a3': 'Da, e PWA — instalabil pe iOS și Android, funcționează și offline.',
    'faq.q4': 'Când vine tier-ul Pro?',
    'faq.a4': 'Planificăm lansarea în Q3 2026. Înscrie-te mai sus pentru acces anticipat.',
    // CTA section
    'cta.heading': 'Gata să calculezi?',
    'cta.body': 'Alătură-te inginerilor și studenților care folosesc StructCalc.',
    'cta.button': 'Începe gratuit',
    // Footer
    'footer.copyright': '© 2026 StructCalc. Toate drepturile rezervate.',
    // Accessibility
    'a11y.darkmode': 'Comută modul întunecat',
    'a11y.skip': 'Sari la conținut',
  },
  en: {
    // Nav
    'nav.logo': 'StructCalc',
    'nav.features': 'Features',
    'nav.pricing': 'Pricing',
    'nav.faq': 'FAQ',
    'nav.cta': 'Get Started Free',
    // Hero
    'hero.headline': 'Structural analysis, reinvented',
    'hero.subheadline': 'Professional software for 2D structural analysis — free for students and engineers.',
    'hero.cta.primary': 'Try for free',
    'hero.cta.secondary': 'See how it works',
    // Features
    'features.heading': 'Why StructCalc?',
    'features.1.title': 'Precise calculation',
    'features.1.body': 'Verified algorithms for 2D bars, beams and frames.',
    'features.2.title': 'PDF Export',
    'features.2.body': 'Professional reports ready to present.',
    'features.3.title': 'Dark mode & PWA',
    'features.3.body': 'Works offline, on any device.',
    // Pricing
    'pricing.heading': 'Pricing',
    'pricing.badge': 'Available Q3 2026',
    'pricing.locked': 'Unlocking soon',
    'pricing.free.name': 'Free',
    'pricing.free.tagline': 'For students and solo users',
    // Email capture
    'email.label': 'Be the first to know',
    'email.placeholder': 'your@email.com',
    'email.cta': 'Notify me',
    'email.success': "You're on the list! We'll notify you at launch.",
    'email.error': 'Enter a valid email address.',
    // FAQ
    'faq.heading': 'Frequently asked questions',
    'faq.q1': 'What types of structures can I analyze?',
    'faq.a1': 'StructCalc supports bars, simply supported beams, 2D frames and rigid-joint structures.',
    'faq.q2': 'Is it really free?',
    'faq.a2': 'Yes. The Free tier never expires and requires no card.',
    'faq.q3': 'Does it work on mobile?',
    'faq.a3': "Yes, it's a PWA — installable on iOS and Android, works offline too.",
    'faq.q4': 'When is the Pro tier coming?',
    'faq.a4': "We're planning launch in Q3 2026. Sign up above for early access.",
    // CTA section
    'cta.heading': 'Ready to calculate?',
    'cta.body': 'Join the engineers and students using StructCalc.',
    'cta.button': 'Get Started Free',
    // Footer
    'footer.copyright': '© 2026 StructCalc. All rights reserved.',
    // Accessibility
    'a11y.darkmode': 'Toggle dark mode',
    'a11y.skip': 'Skip to content',
  },
}
```

**Step 4 — Create `frontend/src/hooks/useTheme.ts`:**
```typescript
import { useState, useEffect, useCallback } from 'react'

type Theme = 'light' | 'dark'

const STORAGE_KEY = 'structcalc-theme'

export function useTheme() {
  const [theme, setTheme] = useState<Theme>(() => {
    const stored = localStorage.getItem(STORAGE_KEY) as Theme | null
    if (stored === 'dark' || stored === 'light') return stored
    return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
  })

  useEffect(() => {
    const root = document.documentElement
    if (theme === 'dark') {
      root.classList.add('dark')
    } else {
      root.classList.remove('dark')
    }
    localStorage.setItem(STORAGE_KEY, theme)
  }, [theme])

  const toggleTheme = useCallback(() => {
    setTheme(prev => (prev === 'dark' ? 'light' : 'dark'))
  }, [])

  return { theme, toggleTheme }
}
```

**Step 5 — Create `frontend/src/hooks/useLang.ts`:**
```typescript
import { useState, useEffect, useCallback } from 'react'
import { translations, type Lang } from '@/lib/i18n'

const STORAGE_KEY = 'structcalc-lang'

export function useLang() {
  const [lang, setLang] = useState<Lang>(() => {
    const stored = localStorage.getItem(STORAGE_KEY) as Lang | null
    return stored === 'en' ? 'en' : 'ro'
  })

  useEffect(() => {
    document.documentElement.setAttribute('lang', lang)
    localStorage.setItem(STORAGE_KEY, lang)
  }, [lang])

  const toggleLang = useCallback(() => {
    setLang(prev => (prev === 'ro' ? 'en' : 'ro'))
  }, [])

  const t = useCallback((key: string): string => {
    return translations[lang][key] ?? key
  }, [lang])

  return { lang, toggleLang, t }
}
```

After creating all files, fill in the test stubs in `useTheme.test.ts` and `useLang.test.ts` to make them pass (replace `it.todo` with real tests that import and test the hooks).
  </action>
  <verify>
    <automated>cd /c/Users/Rares/proiect-cadre/frontend && npx vitest run src/__tests__/useTheme.test.ts src/__tests__/useLang.test.ts --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/index.css` contains `--brand-bg: #F5F5F7` in `:root` block
    - `frontend/src/index.css` contains `--brand-bg: #000000` in `.dark` block
    - `frontend/src/index.css` contains `--brand-accent: #2563EB` in `:root` block
    - `frontend/src/index.css` contains `prefers-reduced-motion` media query block
    - `frontend/index.html` contains `lang="ro"` on `<html>` tag
    - `frontend/index.html` contains `localStorage.getItem('structcalc-theme')` inside a `<script>` in `<head>`
    - `frontend/index.html` contains `<link rel="manifest"` in `<head>`
    - `frontend/index.html` contains `<title>StructCalc` (not `<title>frontend`)
    - `frontend/src/lib/i18n.ts` contains `'hero.headline': 'Calculul structural, reinventat'` (ro) and `'hero.headline': 'Structural analysis, reinvented'` (en)
    - `frontend/src/hooks/useTheme.ts` contains `localStorage.setItem('structcalc-theme'`
    - `frontend/src/hooks/useLang.ts` contains `document.documentElement.setAttribute('lang'`
    - `cd frontend && npx vitest run src/__tests__/useTheme.test.ts src/__tests__/useLang.test.ts` exits 0
  </acceptance_criteria>
  <done>index.css has brand tokens, index.html has blocking script + lang="ro", i18n.ts exports all 30+ keys in both languages, useTheme and useLang hooks pass their tests.</done>
</task>

<task type="auto">
  <name>Task 3: Build Navbar and Footer layout components</name>
  <read_first>
    - frontend/src/hooks/useTheme.ts (import and use)
    - frontend/src/hooks/useLang.ts (import and use)
    - frontend/src/components/ui/button.tsx (Button variants: ghost for nav links, default for CTA)
    - .planning/phases/01-design-sistem-landing-page/01-UI-SPEC.md (Component Inventory, Spacing Scale, Color sections)
    - .planning/phases/01-design-sistem-landing-page/01-CONTEXT.md (D-15, D-16, D-29, D-32)
  </read_first>
  <files>
    frontend/src/components/layout/Navbar.tsx,
    frontend/src/components/layout/Footer.tsx
  </files>
  <action>
**Create `frontend/src/components/layout/Navbar.tsx`:**

Requirements from UI-SPEC and decisions:
- Sticky top, height 64px (--space-3xl), z-index 50
- Background: `bg-[var(--brand-card)]/80 backdrop-blur-md border-b border-[var(--brand-border)]`
- Logo left: "StructCalc" in `font-bold text-[var(--brand-text)]`
- Nav links center/right: Funcții, Prețuri, FAQ — `ghost` Button variant, scroll to section IDs `#features`, `#pricing`, `#faq`
- Smooth scroll via `document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })`
- Active nav link: underline indicator using `text-[var(--brand-accent)] underline` when section is in view (use IntersectionObserver or simple click-based active state)
- Language toggle: text button "RO / EN" — clicking toggles between languages via `toggleLang()`
- Dark mode toggle: icon-only Button (`size="icon"`, `variant="ghost"`) with `lucide-react` `Sun` icon when dark, `Moon` icon when light; `aria-label` comes from `t('a11y.darkmode')`; touch target minimum 44x44px
- Nav CTA button right: "Începe gratuit" / "Get Started Free" using `default` Button variant with `bg-[var(--brand-accent)] text-white hover:bg-[var(--brand-accent)]/90`
- At viewport < 400px: show only logo + CTA button (hide nav links and toggles using `hidden sm:flex` or similar)

```tsx
import { Sun, Moon } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { useTheme } from '@/hooks/useTheme'
import { useLang } from '@/hooks/useLang'

export function Navbar() {
  const { theme, toggleTheme } = useTheme()
  const { lang, toggleLang, t } = useLang()

  const scrollTo = (id: string) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })
  }

  return (
    <nav
      role="navigation"
      aria-label={lang === 'ro' ? 'Navigare principală' : 'Main navigation'}
      className="fixed top-0 left-0 right-0 z-50 h-16 flex items-center px-6
                 bg-[var(--brand-card)]/80 backdrop-blur-md
                 border-b border-[var(--brand-border)]"
    >
      <div className="flex items-center justify-between w-full max-w-[1200px] mx-auto">
        {/* Logo */}
        <span className="font-bold text-[var(--brand-text)] text-lg select-none">
          {t('nav.logo')}
        </span>

        {/* Nav links — hidden below 400px */}
        <div className="hidden min-[400px]:flex items-center gap-1">
          {(['features', 'pricing', 'faq'] as const).map((id) => (
            <Button
              key={id}
              variant="ghost"
              size="sm"
              onClick={() => scrollTo(id)}
              className="text-[var(--brand-text)] hover:text-[var(--brand-accent)]
                         text-sm font-normal transition-colors duration-150"
            >
              {t(`nav.${id}`)}
            </Button>
          ))}
        </div>

        {/* Right controls */}
        <div className="flex items-center gap-2">
          {/* Language toggle — hidden below 400px */}
          <Button
            variant="ghost"
            size="sm"
            onClick={toggleLang}
            className="hidden min-[400px]:inline-flex text-[var(--brand-muted)]
                       hover:text-[var(--brand-text)] text-sm font-normal"
            aria-label={lang === 'ro' ? 'Switch to English' : 'Schimbă în română'}
          >
            {lang === 'ro' ? 'EN' : 'RO'}
          </Button>

          {/* Dark mode toggle — minimum 44x44px touch target */}
          <Button
            variant="ghost"
            size="icon"
            onClick={toggleTheme}
            aria-label={t('a11y.darkmode')}
            className="min-w-[44px] min-h-[44px] text-[var(--brand-text)]"
          >
            {theme === 'dark' ? <Sun className="size-5" /> : <Moon className="size-5" />}
          </Button>

          {/* CTA */}
          <Button
            size="sm"
            onClick={() => scrollTo('cta')}
            className="bg-[var(--brand-accent)] text-white hover:bg-[var(--brand-accent)]/90
                       transition-colors duration-150"
          >
            {t('nav.cta')}
          </Button>
        </div>
      </div>
    </nav>
  )
}
```

**Create `frontend/src/components/layout/Footer.tsx`:**
```tsx
import { useLang } from '@/hooks/useLang'

export function Footer() {
  const { t } = useLang()

  const scrollTo = (id: string) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })
  }

  return (
    <footer className="border-t border-[var(--brand-border)] bg-[var(--brand-bg)]">
      <div className="max-w-[1200px] mx-auto px-6 py-8 flex flex-col sm:flex-row
                      items-center justify-between gap-4">
        <span className="font-bold text-[var(--brand-text)]">StructCalc</span>

        <nav aria-label="Footer navigation" className="flex gap-4">
          {(['features', 'pricing', 'faq'] as const).map((id) => (
            <button
              key={id}
              onClick={() => scrollTo(id)}
              className="text-sm text-[var(--brand-muted)] hover:text-[var(--brand-text)]
                         transition-colors duration-150 cursor-pointer"
            >
              {t(`nav.${id}`)}
            </button>
          ))}
        </nav>

        <p className="text-sm text-[var(--brand-muted)]">{t('footer.copyright')}</p>
      </div>
    </footer>
  )
}
```
  </action>
  <verify>
    <automated>cd /c/Users/Rares/proiect-cadre/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/layout/Navbar.tsx` exists and contains `aria-label={t('a11y.darkmode')}`
    - `frontend/src/components/layout/Navbar.tsx` contains `scrollIntoView({ behavior: 'smooth' })`
    - `frontend/src/components/layout/Navbar.tsx` contains `min-h-[44px]` (44px touch target on dark mode toggle)
    - `frontend/src/components/layout/Footer.tsx` exists and contains `t('footer.copyright')`
    - `cd frontend && npx tsc --noEmit` exits 0 (no TypeScript errors)
    - `cd frontend && npx vitest run --reporter=dot` exits 0 (stubs still passing)
  </acceptance_criteria>
  <done>Navbar and Footer components compile without TypeScript errors, use useTheme + useLang hooks, and meet all accessibility requirements from UI-SPEC.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `cd frontend && npx vitest run` — all 6 test files found, suite exits 0
2. `cd frontend && npx tsc --noEmit` — 0 TypeScript errors
3. `grep 'structcalc-theme' frontend/src/hooks/useTheme.ts` — found
4. `grep 'structcalc-lang' frontend/src/hooks/useLang.ts` — found
5. `grep 'lang="ro"' frontend/index.html` — found
6. `grep 'localStorage.getItem' frontend/index.html` — found (blocking script)
7. `grep 'brand-bg.*#F5F5F7' frontend/src/index.css` — found
8. `grep 'brand-bg.*#000000' frontend/src/index.css` — found in .dark block
</verification>

<success_criteria>
- Vitest infrastructure installed and 6 test stub files exist, suite green
- Brand tokens (--brand-bg, --brand-accent, --brand-text, --brand-muted, --brand-border) defined in both :root and .dark
- index.html has lang="ro", FOUC prevention blocking script, manifest link, Apple touch icon meta tags
- i18n.ts exports translations for all 30+ keys in both 'ro' and 'en'
- useTheme hook uses localStorage key 'structcalc-theme', toggles .dark on <html>
- useLang hook uses localStorage key 'structcalc-lang', updates document.documentElement.lang
- Navbar and Footer compile, render, and use both hooks
- TypeScript clean (0 errors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-sistem-landing-page/01-01-SUMMARY.md` with:
- What was built (list of files created/modified)
- Key decisions made (token values chosen, hook signatures)
- Interfaces exported (useTheme, useLang, i18n.ts exports) — Wave 1 plans depend on these
- Any issues encountered
</output>
