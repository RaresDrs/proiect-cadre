---
phase: 00-setup-proiect
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/api/v1/__init__.py
  - backend/app/api/v1/router.py
  - backend/app/api/v1/health.py
  - backend/app/api/v1/beams.py
  - backend/app/api/v1/sections.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/beam.py
  - backend/app/schemas/section.py
  - backend/app/services/__init__.py
  - backend/app/services/beam_solver.py
  - backend/app/services/section_calc.py
  - backend/requirements.txt
  - backend/runtime.txt
  - backend/railway.json
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/test_health.py
  - backend/tests/test_beams.py
  - backend/tests/test_sections.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /health returneaza {\"status\": \"ok\", \"service\": \"StructCalc API\"}"
    - "POST /api/v1/beams/solve cu o grinda simplu rezemata returneaza reactiuni si diagrame N/V/M"
    - "POST /api/v1/sections/properties cu dreptunghi returneaza A, Ix, Wx, ix corecte"
    - "pytest backend/tests/ -x -q ruleaza cu toate testele verzi"
    - "uvicorn app.main:app --app-dir backend porneste fara ImportError"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI app cu CORS si routere incluse"
      contains: "CORSMiddleware"
    - path: "backend/app/services/beam_solver.py"
      provides: "Functie pura solve_beam() fara st.session_state"
      contains: "SystemElements"
    - path: "backend/app/services/section_calc.py"
      provides: "Functie pura calculate_section_properties() cu numpy"
      contains: "def calculate_section"
    - path: "backend/requirements.txt"
      provides: "Dependinte Python 3.12-compatible"
      contains: "anastruct"
    - path: "backend/runtime.txt"
      provides: "Versiune Python pentru Railway"
      contains: "python-3.12"
    - path: "backend/tests/test_beams.py"
      provides: "Test automat pentru beam solver"
      contains: "def test_"
  key_links:
    - from: "backend/app/api/v1/beams.py"
      to: "backend/app/services/beam_solver.py"
      via: "from app.services.beam_solver import solve_beam"
      pattern: "from app.services.beam_solver"
    - from: "backend/app/api/v1/router.py"
      to: "backend/app/main.py"
      via: "app.include_router(api_router, prefix=\"/api/v1\")"
      pattern: "include_router.*api_router"
    - from: "backend/app/services/beam_solver.py"
      to: "anastruct.SystemElements"
      via: "from anastruct import SystemElements"
      pattern: "from anastruct import SystemElements"
---

<objective>
Creeaza backend-ul FastAPI complet cu calcule migrate din app.py, endpoint-uri REST, si teste automatizate.

Purpose: Backend-ul expune calculele FEM (anastruct) ca API REST stateless. Fiecare request contine toata starea necesara — fara `st.session_state`. Aceasta este migratia critica din monolith Streamlit spre arhitectura moderna.

Output:
- Director `backend/` cu structura FastAPI completa (main, api, schemas, services, tests)
- `GET /health` endpoint functional
- `POST /api/v1/beams/solve` cu calcul anastruct extras din app.py liniile 462-525
- `POST /api/v1/sections/properties` cu calcule NumPy extrase din app.py liniile 951-1116
- `pytest backend/tests/ -x` trece toate testele
- `backend/runtime.txt` cu `python-3.12` pentru Railway deploy
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-setup-proiect/00-RESEARCH.md

<interfaces>
<!-- Structura corecta a directorului backend/ -->
backend/
  app/
    main.py          <- FastAPI() entry point
    core/
      config.py      <- Settings cu CORS_ORIGINS
    api/
      v1/
        __init__.py
        router.py    <- include_router(beams), include_router(sections), include_router(health)
        health.py    <- GET /health
        beams.py     <- POST /beams/solve
        sections.py  <- POST /sections/properties
    schemas/
      __init__.py
      beam.py        <- BeamInput, BeamResult, Support, PointLoad, DiagramPoint
      section.py     <- SectionInput, SectionResult
    services/
      __init__.py
      beam_solver.py <- solve_beam(data: BeamInput) -> BeamResult
      section_calc.py<- calculate_section_properties(data: SectionInput) -> SectionResult
  tests/
    __init__.py
    test_health.py
    test_beams.py
    test_sections.py
  requirements.txt
  runtime.txt        <- "python-3.12"
  railway.json
  pytest.ini

<!-- Cod extras din app.py pentru beam_solver.py (liniile 462-525) -->
<!-- Logica anastruct: -->
ss_fem = SystemElements(EI=E*I_sec, EA=E*A_sec)
# add_element cu coordonate 2D
# add_support_hinged / add_support_roll / add_support_fixed
# q_load pentru sarcini distribuite
# point_load / moment_load pentru forte concentrate
ss_fem.solve()
# get_node_results_system returneaza {"Fx", "Fy", "Tz", "ux", "uy", "phi_z"}
# element_map[i+1].bending_moment, .shear_force, .N_1, .N_2

<!-- ATENTIE: anastruct returneaza numpy.float64, nu float -->
<!-- Solutie: float(r["Fy"]) inainte de a pune in Pydantic model -->
<!-- SAU: configureaza custom JSON encoder in FastAPI -->

<!-- Calcule sectiuni din app.py (linii 951-965, 1081-1097, 1115-1116) -->
<!-- Dreptunghi: A=b*h, Ix=b*h^3/12, Iy=h*b^3/12, Wx=Ix/(h/2), ix=sqrt(Ix/A) -->
<!-- Cerc: A=pi*d^2/4, Ix=pi*d^4/64, Wx=Ix/(d/2), Ip=pi*d^4/32 -->
<!-- Inel: Ix=pi*(d^4-di^4)/64 -->

<!-- Schema Pydantic completa pentru BeamInput/BeamResult -->
class Support(BaseModel):
    x: float       # pozitie pe bara (m) — 0 <= x <= length
    type: int      # 1=articulatie(pin), 2=reazem_mobil(roller), 3=incastrare(fixed)

class PointLoad(BaseModel):
    x: float       # pozitie pe bara (m)
    fx: float = 0.0
    fy: float = 0.0

class BeamInput(BaseModel):
    length: float           # L in metri (> 0)
    angle_deg: float = 0.0  # inclinare bara fata de orizontala (grade)
    supports: List[Support]
    point_loads: List[PointLoad] = []
    distributed_load: float = 0.0   # q uniform (kN/m), pozitiv = in jos
    q_start: float = 0.0            # start pozitie sarcina distribuita (m)
    q_end: Optional[float] = None   # end pozitie (default = length)
    EI: float = 21000.0             # rigiditate la incovoiere (kN*m^2)
    EA: float = 2100000.0           # rigiditate axiala (kN)

class DiagramPoint(BaseModel):
    x: float
    N: float
    V: float
    M: float

class BeamResult(BaseModel):
    reactions: Dict[str, float]    # ex: {"x=0.0_Fy": 50.0, "x=6.0_Fy": 50.0}
    diagrams: List[DiagramPoint]
    max_M: float
    max_V: float
    max_N: float
    deflection: List[Dict[str, float]]  # [{"x": 0.0, "ux": 0.0, "uy": -0.001}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Creeaza structura backend si implementeaza serviciile de calcul</name>
  <files>
    backend/app/main.py
    backend/app/core/config.py
    backend/app/api/v1/__init__.py
    backend/app/api/v1/router.py
    backend/app/api/v1/health.py
    backend/app/api/v1/beams.py
    backend/app/api/v1/sections.py
    backend/app/schemas/__init__.py
    backend/app/schemas/beam.py
    backend/app/schemas/section.py
    backend/app/services/__init__.py
    backend/app/services/beam_solver.py
    backend/app/services/section_calc.py
    backend/requirements.txt
    backend/runtime.txt
    backend/railway.json
    backend/pytest.ini
    backend/tests/__init__.py
    backend/tests/test_health.py
    backend/tests/test_beams.py
    backend/tests/test_sections.py
  </files>

  <read_first>
    - C:/Users/Rares/proiect-cadre/app.py (liniile 448-525 pentru beam solver, liniile 951-965 si 1081-1116 pentru sectiuni)
    - C:/Users/Rares/proiect-cadre/.planning/phases/00-setup-proiect/00-RESEARCH.md (sectiunile "Pattern 1: FastAPI Service Layer", "Migration Strategy", "Common Pitfalls 3 si 4", "Code Examples")
  </read_first>

  <behavior>
    - Test 1 (health): GET /health returneaza status=200, body={"status":"ok","service":"StructCalc API"}
    - Test 2 (beam simplu rezemat): POST /api/v1/beams/solve cu length=6, supports=[{x:0,type:1},{x:6,type:2}], distributed_load=10 returneaza reactions cu Fy total = 60 kN (6m * 10 kN/m), max_M ≈ 45 kNm (q*L^2/8 = 10*36/8 = 45)
    - Test 3 (sectiune dreptunghi): POST /api/v1/sections/properties cu shape="rectangle", b=20, h=40 returneaza A=800, Ix=106666.67, Wx=5333.33, ix=11.547
    - Test 4 (validare): POST /api/v1/beams/solve cu length=-1 returneaza status 422 (Pydantic validation error)
  </behavior>

  <action>
    **Pasul 1 — Creeaza structura de directoare:**
    ```bash
    mkdir -p backend/app/api/v1
    mkdir -p backend/app/schemas
    mkdir -p backend/app/services
    mkdir -p backend/app/core
    mkdir -p backend/tests
    touch backend/app/__init__.py
    touch backend/app/api/__init__.py
    touch backend/app/api/v1/__init__.py
    touch backend/app/schemas/__init__.py
    touch backend/app/services/__init__.py
    touch backend/tests/__init__.py
    ```

    **Pasul 2 — Creeaza `backend/requirements.txt`:**
    ```
    fastapi==0.135.3
    uvicorn[standard]==0.44.0
    pydantic==2.12.5
    numpy==2.4.4
    scipy==1.17.1
    anastruct==1.6.2
    python-multipart
    httpx
    pytest
    pytest-asyncio
    ```

    **Pasul 3 — Creeaza `backend/runtime.txt`:**
    ```
    python-3.12
    ```
    (O singura linie — Railway foloseste asta pentru versiunea Python)

    **Pasul 4 — Creeaza `backend/railway.json`:**
    ```json
    {
      "$schema": "https://railway.com/railway.schema.json",
      "build": {
        "builder": "NIXPACKS"
      },
      "deploy": {
        "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
        "restartPolicyType": "ON_FAILURE"
      }
    }
    ```

    **Pasul 5 — Creeaza `backend/pytest.ini`:**
    ```ini
    [pytest]
    testpaths = tests
    asyncio_mode = auto
    ```

    **Pasul 6 — Creeaza `backend/app/core/config.py`:**
    ```python
    from pydantic_settings import BaseSettings
    from typing import List

    class Settings(BaseSettings):
        CORS_ORIGINS: List[str] = [
            "http://localhost:5173",   # Vite dev server
            "http://localhost:3000",
            "https://*.vercel.app",    # Vercel deploy — actualizat in Faza 3
        ]
        API_V1_PREFIX: str = "/api/v1"
        PROJECT_NAME: str = "StructCalc API"

    settings = Settings()
    ```
    NOTA: Daca `pydantic-settings` nu e instalat, importa simplu `from pydantic import BaseModel` si foloseste o clasa normala cu atributele de mai sus ca valori implicite.

    **Pasul 7 — Creeaza `backend/app/schemas/beam.py`** cu schema completa din sectiunea `<interfaces>` de mai sus (Support, PointLoad, BeamInput, DiagramPoint, BeamResult).
    Adauga validator Pydantic v2 pentru `length`:
    ```python
    from pydantic import BaseModel, field_validator
    from typing import List, Optional, Dict

    class BeamInput(BaseModel):
        # ... toate campurile din interfaces ...

        @field_validator('length')
        @classmethod
        def length_must_be_positive(cls, v):
            if v <= 0:
                raise ValueError('Lungimea barei trebuie sa fie pozitiva')
            return v
    ```

    **Pasul 8 — Creeaza `backend/app/schemas/section.py`:**
    ```python
    from pydantic import BaseModel
    from typing import Optional, Literal

    class SectionInput(BaseModel):
        shape: Literal['rectangle', 'circle', 'hollow_circle', 'T', 'I']
        b: Optional[float] = None   # latime (cm)
        h: Optional[float] = None   # inaltime (cm)
        d: Optional[float] = None   # diametru exterior (cm)
        d_inner: Optional[float] = None  # diametru interior pentru inel (cm)

    class SectionResult(BaseModel):
        A: float     # arie sectiune (cm2)
        Ix: float    # moment de inertie in raport cu axa x (cm4)
        Iy: float    # moment de inertie in raport cu axa y (cm4)
        Wx: float    # modul de rezistenta la incovoiere (cm3)
        ix: float    # raza de giratie (cm)
        Ip: Optional[float] = None  # moment polar (cm4) — doar pentru cerc
    ```

    **Pasul 9 — Creeaza `backend/app/services/beam_solver.py`:**
    Extrage logica din app.py liniile 454-522. Transforma in functie pura fara `st.session_state`:
    ```python
    import numpy as np
    from anastruct import SystemElements
    from app.schemas.beam import BeamInput, BeamResult, DiagramPoint
    from typing import Dict, List

    def solve_beam(data: BeamInput) -> BeamResult:
        """
        Rezolva o grinda 2D folosind FEM (anastruct).
        Fara dependinta de Streamlit sau session_state.
        Toate inputurile vin din data: BeamInput.
        """
        L = data.length
        th = np.radians(data.angle_deg)
        c_ang, s_ang = np.cos(th), np.sin(th)

        q_end = data.q_end if data.q_end is not None else L

        # Construieste lista de noduri FEM
        raw_nodes = set([0.0, L])
        for s in data.supports:
            raw_nodes.add(round(s.x, 6))
        for f in data.point_loads:
            raw_nodes.add(round(f.x, 6))
        if data.distributed_load != 0:
            raw_nodes.add(round(data.q_start, 6))
            raw_nodes.add(round(q_end, 6))
        nodes_s = sorted(raw_nodes)
        nn = len(nodes_s)
        ne = nn - 1

        def nidx(xv):
            return nodes_s.index(round(xv, 6))

        # Initializeaza sistemul FEM
        ss_fem = SystemElements(EI=data.EI, EA=data.EA)
        for i in range(ne):
            x1, x2 = nodes_s[i], nodes_s[i + 1]
            ss_fem.add_element(
                location=[[x1 * c_ang, x1 * s_ang], [x2 * c_ang, x2 * s_ang]]
            )

        # Adauga reazeme
        for s in data.supports:
            nid = nidx(s.x) + 1
            if s.type == 1:
                ss_fem.add_support_hinged(node_id=nid)
            elif s.type == 2:
                ss_fem.add_support_roll(node_id=nid)
            elif s.type == 3:
                ss_fem.add_support_fixed(node_id=nid)

        # Sarcina distribuita
        if abs(data.distributed_load) > 1e-9 and q_end > data.q_start:
            q_sign = -data.distributed_load  # anastruct: negativ = in jos
            for eid in range(1, ne + 1):
                mid = (nodes_s[eid - 1] + nodes_s[eid]) / 2
                if data.q_start - 1e-6 <= mid <= q_end + 1e-6:
                    ss_fem.q_load(element_id=eid, q=q_sign, direction='y')

        # Forte concentrate si momente
        for f in data.point_loads:
            nid = nidx(f.x) + 1
            if abs(f.fx) > 1e-9:
                ss_fem.point_load(node_id=nid, Fx=f.fx)
            if abs(f.fy) > 1e-9:
                ss_fem.point_load(node_id=nid, Fy=f.fy)

        # Rezolva sistemul
        ss_fem.solve()

        # Extrage reactiunile — float() pentru a converti numpy.float64
        reactions: Dict[str, float] = {}
        for s in data.supports:
            ni = nidx(s.x)
            nid = ni + 1
            r = ss_fem.get_node_results_system(node_id=nid)
            prefix = f"x={s.x:.2f}"
            reactions[f"{prefix}_Fx"] = float(r["Fx"])
            reactions[f"{prefix}_Fy"] = float(r["Fy"])
            reactions[f"{prefix}_Mz"] = float(r["Tz"])

        # Extrage deplasarile
        deflection = []
        for i in range(nn):
            nid = i + 1
            r = ss_fem.get_node_results_system(node_id=nid)
            deflection.append({
                "x": float(nodes_s[i]),
                "ux": float(r["ux"]),
                "uy": float(r["uy"])
            })

        # Construieste diagramele N, V, M
        diagrams: List[DiagramPoint] = []
        for i in range(ne):
            el = ss_fem.element_map[i + 1]
            npts = len(el.bending_moment)
            xs = np.linspace(nodes_s[i], nodes_s[i + 1], npts)
            N_arr = np.linspace(el.N_1, el.N_2, npts)
            for j in range(npts):
                diagrams.append(DiagramPoint(
                    x=float(xs[j]),
                    N=float(N_arr[j]),
                    V=float(el.shear_force[j]),
                    M=float(el.bending_moment[j])
                ))

        M_vals = [d.M for d in diagrams]
        V_vals = [d.V for d in diagrams]
        N_vals = [d.N for d in diagrams]

        return BeamResult(
            reactions=reactions,
            diagrams=diagrams,
            max_M=float(max(abs(m) for m in M_vals)),
            max_V=float(max(abs(v) for v in V_vals)),
            max_N=float(max(abs(n) for n in N_vals)) if N_vals else 0.0,
            deflection=deflection
        )
    ```

    **Pasul 10 — Creeaza `backend/app/services/section_calc.py`:**
    Extrage calculele numerice din app.py liniile 951-1116:
    ```python
    import numpy as np
    from app.schemas.section import SectionInput, SectionResult

    def calculate_section_properties(data: SectionInput) -> SectionResult:
        """
        Calculeaza proprietatile geometrice ale sectiunii transversale.
        Formule extrase din app.py modul Rezistenta Materialelor (liniile 951-1116).
        """
        if data.shape == 'rectangle':
            b, h = data.b, data.h
            A = b * h
            Ix = b * h**3 / 12
            Iy = h * b**3 / 12
            Wx = Ix / (h / 2)
            ix = float(np.sqrt(Ix / A))
            return SectionResult(A=A, Ix=Ix, Iy=Iy, Wx=Wx, ix=ix)

        elif data.shape == 'circle':
            d = data.d
            r = d / 2
            A = float(np.pi * r**2)
            Ix = float(np.pi * d**4 / 64)
            Iy = Ix
            Wx = Ix / r
            ix = d / 4
            Ip = float(np.pi * d**4 / 32)
            return SectionResult(A=A, Ix=Ix, Iy=Iy, Wx=Wx, ix=ix, Ip=Ip)

        elif data.shape == 'hollow_circle':
            d, di = data.d, data.d_inner
            A = float(np.pi * (d**2 - di**2) / 4)
            Ix = float(np.pi * (d**4 - di**4) / 64)
            Iy = Ix
            Wx = Ix / (d / 2)
            ix = float(np.sqrt(Ix / A))
            Ip = float(np.pi * (d**4 - di**4) / 32)
            return SectionResult(A=A, Ix=Ix, Iy=Iy, Wx=Wx, ix=ix, Ip=Ip)

        else:
            raise ValueError(f"Forma sectiunii '{data.shape}' nu este implementata inca")
    ```

    **Pasul 11 — Creeaza `backend/app/api/v1/health.py`:**
    ```python
    from fastapi import APIRouter

    router = APIRouter(tags=["health"])

    @router.get("/health")
    def health_check():
        return {"status": "ok", "service": "StructCalc API"}
    ```

    **Pasul 12 — Creeaza `backend/app/api/v1/beams.py`:**
    ```python
    from fastapi import APIRouter, HTTPException
    from app.schemas.beam import BeamInput, BeamResult
    from app.services.beam_solver import solve_beam

    router = APIRouter(prefix="/beams", tags=["beams"])

    @router.post("/solve", response_model=BeamResult)
    def calculate_beam(data: BeamInput):
        try:
            return solve_beam(data)
        except Exception as e:
            raise HTTPException(status_code=422, detail=str(e))
    ```

    **Pasul 13 — Creeaza `backend/app/api/v1/sections.py`:**
    ```python
    from fastapi import APIRouter, HTTPException
    from app.schemas.section import SectionInput, SectionResult
    from app.services.section_calc import calculate_section_properties

    router = APIRouter(prefix="/sections", tags=["sections"])

    @router.post("/properties", response_model=SectionResult)
    def get_section_properties(data: SectionInput):
        try:
            return calculate_section_properties(data)
        except ValueError as e:
            raise HTTPException(status_code=422, detail=str(e))
    ```

    **Pasul 14 — Creeaza `backend/app/api/v1/router.py`:**
    ```python
    from fastapi import APIRouter
    from app.api.v1 import beams, sections

    api_router = APIRouter()
    api_router.include_router(beams.router)
    api_router.include_router(sections.router)
    ```

    **Pasul 15 — Creeaza `backend/app/main.py`:**
    ```python
    from fastapi import FastAPI
    from fastapi.middleware.cors import CORSMiddleware
    from app.api.v1.router import api_router
    from app.api.v1.health import router as health_router

    app = FastAPI(
        title="StructCalc API",
        version="0.1.0",
        description="API pentru calcul structural 2D — grinzi si cadre"
    )

    # CORS — permite cereri de la frontend Vite si Vercel
    app.add_middleware(
        CORSMiddleware,
        allow_origins=[
            "http://localhost:5173",
            "http://localhost:3000",
            "https://*.vercel.app",
        ],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    app.include_router(health_router)
    app.include_router(api_router, prefix="/api/v1")
    ```

    **Pasul 16 — Creeaza testele:**

    `backend/tests/test_health.py`:
    ```python
    from fastapi.testclient import TestClient
    from app.main import app

    client = TestClient(app)

    def test_health_returns_ok():
        response = client.get("/health")
        assert response.status_code == 200
        data = response.json()
        assert data["status"] == "ok"
        assert data["service"] == "StructCalc API"
    ```

    `backend/tests/test_beams.py`:
    ```python
    import pytest
    from fastapi.testclient import TestClient
    from app.main import app

    client = TestClient(app)

    def test_beam_simply_supported_uniform_load():
        """Grinda simplu rezemata cu sarcina uniforma q=10 kN/m, L=6m"""
        payload = {
            "length": 6.0,
            "angle_deg": 0.0,
            "supports": [
                {"x": 0.0, "type": 1},
                {"x": 6.0, "type": 2}
            ],
            "point_loads": [],
            "distributed_load": 10.0,
            "q_start": 0.0,
            "q_end": 6.0,
            "EI": 21000.0,
            "EA": 2100000.0
        }
        response = client.post("/api/v1/beams/solve", json=payload)
        assert response.status_code == 200
        data = response.json()
        # Reactiuni: fiecare reazem = q*L/2 = 30 kN
        reactions = data["reactions"]
        fy_sum = sum(v for k, v in reactions.items() if "_Fy" in k)
        assert abs(fy_sum - 60.0) < 0.5, f"Suma reactiuni Fy = {fy_sum}, asteptat ~60"
        # Moment maxim: q*L^2/8 = 10*36/8 = 45 kNm
        assert abs(data["max_M"] - 45.0) < 1.0, f"max_M = {data['max_M']}, asteptat ~45"

    def test_beam_invalid_length():
        payload = {"length": -1.0, "supports": [], "EI": 21000.0, "EA": 2100000.0}
        response = client.post("/api/v1/beams/solve", json=payload)
        assert response.status_code == 422

    def test_beam_result_has_diagrams():
        payload = {
            "length": 4.0,
            "supports": [{"x": 0.0, "type": 1}, {"x": 4.0, "type": 2}],
            "point_loads": [{"x": 2.0, "fx": 0.0, "fy": -20.0}],
            "distributed_load": 0.0,
            "q_start": 0.0,
            "EI": 21000.0,
            "EA": 2100000.0
        }
        response = client.post("/api/v1/beams/solve", json=payload)
        assert response.status_code == 200
        data = response.json()
        assert len(data["diagrams"]) > 0
        assert all("x" in d and "N" in d and "V" in d and "M" in d for d in data["diagrams"])
    ```

    `backend/tests/test_sections.py`:
    ```python
    import pytest
    from fastapi.testclient import TestClient
    from app.main import app

    client = TestClient(app)

    def test_rectangle_section():
        """Dreptunghi b=20cm, h=40cm"""
        payload = {"shape": "rectangle", "b": 20.0, "h": 40.0}
        response = client.post("/api/v1/sections/properties", json=payload)
        assert response.status_code == 200
        data = response.json()
        assert abs(data["A"] - 800.0) < 0.01, f"A = {data['A']}, asteptat 800"
        assert abs(data["Ix"] - 106666.67) < 1.0, f"Ix = {data['Ix']}, asteptat 106666.67"
        assert abs(data["Wx"] - 5333.33) < 1.0, f"Wx = {data['Wx']}, asteptat 5333.33"
        assert abs(data["ix"] - 11.547) < 0.01, f"ix = {data['ix']}, asteptat 11.547"

    def test_circle_section():
        """Cerc d=30cm"""
        import math
        payload = {"shape": "circle", "d": 30.0}
        response = client.post("/api/v1/sections/properties", json=payload)
        assert response.status_code == 200
        data = response.json()
        expected_A = math.pi * 15**2
        assert abs(data["A"] - expected_A) < 0.01
        assert data["Ip"] is not None

    def test_invalid_shape():
        payload = {"shape": "triangle", "b": 10.0}
        response = client.post("/api/v1/sections/properties", json=payload)
        assert response.status_code == 422
    ```

    **Pasul 17 — Instaleaza dependintele si ruleaza testele:**
    ```bash
    cd backend
    pip install -r requirements.txt
    pytest tests/ -x -v
    ```
  </action>

  <verify>
    ```bash
    cd C:/Users/Rares/proiect-cadre/backend && pytest tests/ -x -q 2>&1
    ```
    Output asteptat: toate testele verzi, `X passed` fara erori.

    Verifica si startup:
    ```bash
    cd C:/Users/Rares/proiect-cadre && python -c "import sys; sys.path.insert(0, 'backend'); from app.main import app; print('Import OK')"
    ```
  </verify>

  <acceptance_criteria>
    - `backend/runtime.txt` contine `python-3.12`: `cat backend/runtime.txt`
    - `backend/requirements.txt` contine `anastruct`: `grep "anastruct" backend/requirements.txt`
    - `backend/app/main.py` contine `CORSMiddleware`: `grep "CORSMiddleware" backend/app/main.py`
    - `backend/app/services/beam_solver.py` contine `from anastruct import SystemElements`: `grep "from anastruct" backend/app/services/beam_solver.py`
    - `backend/app/services/beam_solver.py` contine `float(r["Fy"])` sau pattern similar pentru conversie numpy: `grep "float(r\[" backend/app/services/beam_solver.py`
    - `backend/app/api/v1/router.py` contine `include_router(beams.router)`: `grep "beams.router" backend/app/api/v1/router.py`
    - `pytest backend/tests/ -x -q` returneaza exit code 0 cu toate testele verzi
    - `backend/tests/test_beams.py` contine `def test_beam_simply_supported_uniform_load`: `grep "def test_beam_simply" backend/tests/test_beams.py`
  </acceptance_criteria>

  <done>Toate testele backend trec. API porneste fara erori. Calculele din app.py (anastruct beam solver si sectiuni geometrice) sunt disponibile ca endpoint-uri REST stateless.</done>
</task>

</tasks>

<verification>
Verificare finala dupa task:

```bash
# Toate testele verzi
cd C:/Users/Rares/proiect-cadre && pytest backend/tests/ -v

# Structura completa exista
test -f backend/app/main.py && echo "main.py OK"
test -f backend/app/services/beam_solver.py && echo "beam_solver.py OK"
test -f backend/app/services/section_calc.py && echo "section_calc.py OK"
test -f backend/runtime.txt && cat backend/runtime.txt | grep "python-3.12" && echo "runtime.txt OK"
test -f backend/railway.json && echo "railway.json OK"

# Import sanity check
python -c "import sys; sys.path.insert(0,'backend'); from app.main import app; print('FastAPI app import OK')"
```
</verification>

<success_criteria>
1. `pytest backend/tests/ -x -q` returneaza 0 (toate testele verzi)
2. `GET /health` returneaza `{"status": "ok", "service": "StructCalc API"}`
3. `POST /api/v1/beams/solve` cu grinda simplu rezemata q=10kN/m L=6m returneaza max_M ≈ 45 kNm
4. `POST /api/v1/sections/properties` cu dreptunghi b=20 h=40 returneaza A=800 cm2
5. `backend/runtime.txt` contine `python-3.12`
6. `backend/app/services/beam_solver.py` nu contine nicio referinta la `st.session_state` sau `streamlit`
</success_criteria>

<output>
Dupa completare, creeaza `.planning/phases/00-setup-proiect/00-02-SUMMARY.md` cu:
- Fisierele create si structura finala a backend/
- Rezultatul `pytest backend/tests/ -v` (output complet)
- Versiunile efectiv instalate (pip list | grep -E "fastapi|uvicorn|pydantic|anastruct|numpy")
- Orice probleme intampinate cu anastruct sau numpy.float64 si cum s-au rezolvat
- Comanda de verificare rapida: `pytest backend/tests/ -x -q`
</output>
