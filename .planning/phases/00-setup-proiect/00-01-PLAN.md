---
phase: 00-setup-proiect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/components/ui/button.tsx
  - frontend/components.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "`npm run dev` in frontend/ porneste Vite fara erori de compilare"
    - "`npm run build` produce dist/ fara erori TypeScript"
    - "shadcn/ui Button component functioneaza in App.tsx"
    - "Tailwind v4 aplica stiluri (clasa text-red-500 coloreaza textul)"
    - "Path alias @/ rezolva catre src/"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vite config cu @tailwindcss/vite plugin si proxy /api -> localhost:8000"
      contains: "tailwindcss()"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 entry point"
      contains: "@import \"tailwindcss\""
    - path: "frontend/components.json"
      provides: "shadcn/ui config"
      contains: "\"style\""
    - path: "frontend/src/App.tsx"
      provides: "Entry component cu shadcn Button demo"
      contains: "StructCalc"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "import tailwindcss from \"@tailwindcss/vite\""
      pattern: "tailwindcss\\(\\)"
    - from: "frontend/src/index.css"
      to: "Tailwind v4"
      via: "@import directive"
      pattern: "@import \"tailwindcss\""
    - from: "frontend/src/components/ui/button.tsx"
      to: "frontend/src/lib/utils.ts"
      via: "import { cn }"
      pattern: "from.*lib/utils"
---

<objective>
Creeaza scaffold-ul complet React 19 + Vite + TypeScript + Tailwind v4 + shadcn/ui pentru StructCalc.

Purpose: Frontend-ul este baza pe care se construieste toata UI-ul aplicatiei. Trebuie sa fie functional, type-safe si cu design system gata de utilizat din Faza 1.

Output:
- Director `frontend/` cu Vite + React 19 + TypeScript 6 configurat
- Tailwind v4 via `@tailwindcss/vite` (fara tailwind.config.js)
- shadcn/ui initializat cu componenta Button ca proba de functionare
- Proxy Vite `/api` -> `http://localhost:8000` pentru development
- `npm run build` trece fara erori
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-setup-proiect/00-RESEARCH.md

<interfaces>
<!-- Versiuni exacte verificate live 2026-04-08 via npm registry -->
react: 19.2.4
vite: 8.0.7
typescript: 6.0.2
@vitejs/plugin-react: 6.0.1
tailwindcss: 4.2.2
@tailwindcss/vite: 4.2.2
shadcn/ui CLI: npx shadcn@latest

<!-- Structura directorului tinta -->
frontend/
  src/
    components/ui/   <- shadcn/ui (generat de CLI)
    lib/utils.ts     <- cn() helper
    types/           <- TypeScript types API
    pages/           <- Route pages (gol in Phase 0)
    hooks/           <- Custom hooks (gol in Phase 0)
    main.tsx
    App.tsx
    index.css
  public/
  index.html
  vite.config.ts
  tsconfig.json
  tsconfig.app.json
  package.json
  components.json    <- shadcn config

<!-- Pattern critic Tailwind v4: NU tailwind.config.js -->
<!-- vite.config.ts TREBUIE sa contina: -->
import tailwindcss from "@tailwindcss/vite"
plugins: [react(), tailwindcss()]

<!-- index.css TREBUIE sa contina: -->
@import "tailwindcss";
<!-- NU @tailwind base; @tailwind components; etc. -->

<!-- tsconfig.app.json TREBUIE sa contina paths pentru @ alias: -->
"baseUrl": ".",
"paths": { "@/*": ["./src/*"] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initializeaza proiectul Vite React-TS si instaleaza Tailwind v4</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/index.css
  </files>

  <read_first>
    - C:/Users/Rares/proiect-cadre/.planning/phases/00-setup-proiect/00-RESEARCH.md (sectiunile "Standard Stack", "Pattern 2: Vite + Tailwind v4", "Pitfall 2: Tailwind v4 vs v3", "Code Examples: Vite proxy")
    - C:/Users/Rares/proiect-cadre/CLAUDE.md
  </read_first>

  <action>
    Ruleaza comenzile in ordine din radacina repo-ului `C:/Users/Rares/proiect-cadre`:

    **Pasul 1 — Creeaza proiectul Vite:**
    ```bash
    npm create vite@latest frontend -- --template react-ts
    cd frontend
    npm install
    ```

    **Pasul 2 — Instaleaza Tailwind v4:**
    ```bash
    npm install -D tailwindcss@4.2.2 @tailwindcss/vite@4.2.2
    ```
    IMPORTANT: NU instala `autoprefixer` sau `postcss` separat — Tailwind v4 nu le foloseste.
    NU crea `tailwind.config.js` — Tailwind v4 nu il mai foloseste.

    **Pasul 3 — Instaleaza dependinte shadcn (pre-install):**
    ```bash
    npm install -D @types/node
    npm install class-variance-authority clsx tailwind-merge tw-animate-css
    ```

    **Pasul 4 — Rescrie `frontend/vite.config.ts` exact asa:**
    ```typescript
    import path from "path"
    import tailwindcss from "@tailwindcss/vite"
    import react from "@vitejs/plugin-react"
    import { defineConfig } from "vite"

    export default defineConfig({
      plugins: [react(), tailwindcss()],
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "./src"),
        },
      },
      server: {
        proxy: {
          '/api': {
            target: 'http://localhost:8000',
            changeOrigin: true,
          }
        }
      }
    })
    ```

    **Pasul 5 — Rescrie `frontend/src/index.css` exact asa:**
    ```css
    @import "tailwindcss";
    @import "tw-animate-css";

    @custom-variant dark (&:is(.dark *));

    @layer base {
      :root {
        --background: 0 0% 100%;
        --foreground: 222.2 84% 4.9%;
        --primary: 221.2 83.2% 53.3%;
        --primary-foreground: 210 40% 98%;
        --radius: 0.5rem;
      }
      .dark {
        --background: 222.2 84% 4.9%;
        --foreground: 210 40% 98%;
        --primary: 217.2 91.2% 59.8%;
        --primary-foreground: 222.2 47.4% 11.2%;
      }
    }

    * {
      border-color: hsl(var(--border, 214.3 31.8% 91.4%));
    }

    body {
      background-color: hsl(var(--background));
      color: hsl(var(--foreground));
    }
    ```

    **Pasul 6 — Actualizeaza `frontend/tsconfig.app.json`** pentru path alias @/:
    Adauga in `compilerOptions`:
    ```json
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
    ```

    **Pasul 7 — Sterge fisierele generate de Vite care nu se potrivesc:**
    - Sterge `frontend/src/App.css` (se inlocuieste cu Tailwind)
    - Sterge `frontend/public/vite.svg` (optional)

    VERIFICA ca `frontend/src/main.tsx` importa `./index.css`:
    ```typescript
    import './index.css'
    ```
  </action>

  <verify>
    ```bash
    cd C:/Users/Rares/proiect-cadre/frontend && npm run build 2>&1 | tail -5
    ```
    Output asteptat: `dist/index.html` creat, fara erori TypeScript sau Tailwind.
  </verify>

  <acceptance_criteria>
    - `frontend/package.json` contine `"@tailwindcss/vite"` in devDependencies: `grep "@tailwindcss/vite" frontend/package.json`
    - `frontend/package.json` NU contine `"tailwind-config"` sau `"postcss"`: `grep -v "postcss\|tailwind.config" frontend/package.json`
    - `frontend/vite.config.ts` contine exact `tailwindcss()` in plugins: `grep "tailwindcss()" frontend/vite.config.ts`
    - `frontend/src/index.css` incepe cu `@import "tailwindcss"`: `head -1 frontend/src/index.css`
    - `frontend/tsconfig.app.json` contine `"@/*"`: `grep "@/\*" frontend/tsconfig.app.json`
    - NU exista `frontend/tailwind.config.js`: `ls frontend/tailwind.config.js 2>&1 | grep "No such"`
    - `npm run build --prefix frontend` returneaza exit code 0
  </acceptance_criteria>

  <done>Build Vite trece fara erori, Tailwind v4 configurat corect, structura de baza creata.</done>
</task>

<task type="auto">
  <name>Task 2: Initializeaza shadcn/ui si creeaza App.tsx de baza pentru StructCalc</name>
  <files>
    frontend/components.json
    frontend/src/lib/utils.ts
    frontend/src/components/ui/button.tsx
    frontend/src/App.tsx
    frontend/src/types/api.ts
  </files>

  <read_first>
    - frontend/vite.config.ts (verificat la Task 1)
    - frontend/src/index.css (verificat la Task 1)
    - frontend/tsconfig.app.json (verificat la Task 1)
    - C:/Users/Rares/proiect-cadre/.planning/phases/00-setup-proiect/00-RESEARCH.md (sectiunea "Pitfall 2: Tailwind v4 vs v3 CLI shadcn")
  </read_first>

  <action>
    **Pasul 1 — Ruleaza shadcn init din directorul frontend:**
    ```bash
    cd C:/Users/Rares/proiect-cadre/frontend
    npx shadcn@latest init
    ```
    La intrebari, alege:
    - Style: Default
    - Base color: Slate
    - CSS variables: Yes

    VERIFICA dupa init ca `frontend/src/index.css` inca contine `@import "tailwindcss"` (shadcn@latest nu ar trebui sa suprascrie).
    Daca a suprascris cu `@tailwind base`, restaureaza continutul de la Task 1 Pasul 5 si adauga variabilele CSS generate de shadcn.

    **Pasul 2 — Adauga componenta Button:**
    ```bash
    npx shadcn@latest add button
    ```

    **Pasul 3 — Creeaza `frontend/src/types/api.ts`** cu tipurile de baza pentru comunicarea cu FastAPI:
    ```typescript
    // Tipuri pentru StructCalc API — sincronizate cu backend/app/schemas/

    export interface HealthResponse {
      status: string;
      service: string;
    }

    export interface Support {
      x: number;       // pozitie pe bara (m)
      type: number;    // 1=articulatie, 2=reazem mobil, 3=incastrare
    }

    export interface PointLoad {
      x: number;
      fx: number;
      fy: number;
    }

    export interface BeamInput {
      length: number;          // L in metri
      angle_deg: number;       // inclinare bara
      supports: Support[];
      point_loads: PointLoad[];
      distributed_load: number; // q in kN/m
      q_start: number;
      q_end: number;
      EI: number;
      EA: number;
    }

    export interface DiagramPoint {
      x: number;
      N: number;
      V: number;
      M: number;
    }

    export interface BeamResult {
      reactions: Record<string, number>;
      diagrams: DiagramPoint[];
      max_M: number;
      max_V: number;
      deflection: Array<{ x: number; ux: number; uy: number }>;
    }

    export interface SectionInput {
      shape: 'rectangle' | 'circle' | 'hollow_circle' | 'T' | 'I';
      b?: number;   // latime (cm)
      h?: number;   // inaltime (cm)
      d?: number;   // diametru (cm)
      d_inner?: number; // diametru interior (cm)
    }

    export interface SectionResult {
      A: number;    // arie (cm2)
      Ix: number;   // moment de inertie (cm4)
      Iy: number;
      Wx: number;   // modul de rezistenta (cm3)
      ix: number;   // raza de giratie (cm)
    }
    ```

    **Pasul 4 — Rescrie `frontend/src/App.tsx`** ca pagina placeholder pentru StructCalc:
    ```tsx
    import { Button } from "@/components/ui/button"

    function App() {
      return (
        <div className="min-h-screen bg-background text-foreground">
          <header className="border-b px-6 py-4">
            <h1 className="text-2xl font-bold text-primary">
              StructCalc
            </h1>
            <p className="text-sm text-muted-foreground">
              Calculator structural 2D — Stud. Pop Rareș Darius
            </p>
          </header>
          <main className="container mx-auto px-6 py-12 text-center">
            <h2 className="text-4xl font-bold mb-4">
              Bun venit la StructCalc
            </h2>
            <p className="text-muted-foreground mb-8 max-w-md mx-auto">
              Aplicatie web de calcul structural 2D pentru grinzi si cadre.
              Stack: React 19 + FastAPI + anastruct.
            </p>
            <Button size="lg" onClick={() => alert('API: /health')}>
              Verifica conexiunea API
            </Button>
          </main>
        </div>
      )
    }

    export default App
    ```
  </action>

  <verify>
    ```bash
    cd C:/Users/Rares/proiect-cadre/frontend && npm run build 2>&1 | tail -5
    ```
    Si verifica fisierele:
    ```bash
    test -f frontend/components.json && echo "components.json OK"
    test -f frontend/src/lib/utils.ts && echo "utils.ts OK"
    test -f frontend/src/components/ui/button.tsx && echo "button.tsx OK"
    test -f frontend/src/types/api.ts && echo "api.ts OK"
    ```
  </verify>

  <acceptance_criteria>
    - `frontend/components.json` exista si contine `"style"`: `grep "style" frontend/components.json`
    - `frontend/src/lib/utils.ts` exista si contine `cn`: `grep "export.*cn\|function cn" frontend/src/lib/utils.ts`
    - `frontend/src/components/ui/button.tsx` exista: `test -f frontend/src/components/ui/button.tsx && echo OK`
    - `frontend/src/App.tsx` contine `StructCalc`: `grep "StructCalc" frontend/src/App.tsx`
    - `frontend/src/App.tsx` importa Button de la `@/components/ui/button`: `grep "@/components/ui/button" frontend/src/App.tsx`
    - `frontend/src/types/api.ts` contine interfata `BeamInput`: `grep "export interface BeamInput" frontend/src/types/api.ts`
    - `npm run build --prefix frontend` exit code 0 (fara erori TypeScript)
    - `frontend/src/index.css` contine `@import "tailwindcss"` (nu a fost suprascris): `grep "@import \"tailwindcss\"" frontend/src/index.css`
  </acceptance_criteria>

  <done>shadcn/ui initializat, Button component functioneaza, App.tsx placeholder creat, tipuri TypeScript API definite. `npm run build` trece fara erori.</done>
</task>

</tasks>

<verification>
Verificare finala dupa ambele task-uri:

```bash
# Build complet fara erori
npm run build --prefix frontend

# Confirma structura corecta
test -f frontend/vite.config.ts && grep "tailwindcss()" frontend/vite.config.ts && echo "Tailwind v4 OK"
test -f frontend/src/index.css && head -1 frontend/src/index.css | grep "@import" && echo "CSS import OK"
test -f frontend/components.json && echo "shadcn OK"
test -f frontend/src/types/api.ts && echo "Types OK"

# Confirma ca NU exista fisiere deprecate
test ! -f frontend/tailwind.config.js && echo "No deprecated tailwind.config.js"
test ! -f frontend/postcss.config.js && echo "No deprecated postcss.config.js"
```
</verification>

<success_criteria>
1. `npm run build --prefix frontend` completeaza fara erori sau warning-uri TypeScript
2. `frontend/vite.config.ts` contine `tailwindcss()` in plugins (Tailwind v4 pattern)
3. `frontend/src/index.css` contine `@import "tailwindcss"` (nu `@tailwind base`)
4. `frontend/src/components/ui/button.tsx` exista (shadcn functional)
5. `frontend/src/types/api.ts` exporta `BeamInput`, `BeamResult`, `SectionInput`, `SectionResult`
6. NU exista `frontend/tailwind.config.js`
</success_criteria>

<output>
Dupa completare, creeaza `.planning/phases/00-setup-proiect/00-01-SUMMARY.md` cu:
- Ce a fost creat (fisiere, versiuni exacte din package.json)
- Versiunile instalate efectiv (npm list --depth=0 in frontend/)
- Orice devieri de la plan si motivul
- Comanda de verificare rapida: `npm run build --prefix frontend`
</output>
